《식객 허영만의 백반기행》의 전체 회차(1~18페이지 분량) 데이터를 자동으로 수집하고 정리하는 크롤링 프로그램 구현 방안을 수립해 드립니다.

TV조선 홈페이지 및 게시판 구조의 특성을 고려하여, 안정적이고 효율적으로 데이터를 수집할 수 있는 아키텍처와 구현 절차를 정리했습니다.

---

### 🛠️ 1. 기술 스택 선정 (Python 기준)

* **크롤링 엔진:** `requests` + `BeautifulSoup` (정적 페이지이거나 API 호출 형태일 경우) 또는 `Selenium` / `Playwright` (동적 렌더링이나 로그인/세션 유지가 필요할 경우)
* **데이터 처리 및 저장:** `pandas` (데이터 구조화 및 정제), `SQLite` 또는 `CSV/Excel` (최종 저장)

---

### 📊 2. 구현 단계별 방안

#### ① 타겟 URL 분석 및 페이지네이션 구조 파악

* 제공해주신 URL 패턴(`.../list.cstv?search_text=&pg=1`)을 보면 `pg` 파라미터를 통해 1부터 18페이지까지 순회할 수 있습니다.
* 각 페이지의 목록 화면에서 [회차 번호, 방송일, 제목(부제/게스트 정보), 상세 링크]를 파싱할 수 있는 HTML 요소(CSS Selector)를 분석합니다.

#### ② 크롤러 로직 설계 (Pipeline)

1. **페이지 순회 (Loop):** `pg=1`부터 `pg=18`까지 반복문을 실행합니다.
2. **목록 수집:** 각 페이지의 게시글 목록을 가져와 개별 회차의 상세 페이지 URL을 추출합니다.
3. **상세 정보 파싱:** 상세 페이지에 접속하여 방문 식당 이름, 지역, 메뉴, 출연 게스트 등의 세부 내용을 추출합니다.
4. **서버 부하 방지:** 요청 사이에 `time.sleep(1)` 등의 지연 시간(Throttle)을 주어 차단을 방지합니다.

#### ③ 예외 처리 및 데이터 정제

* 네트워크 오류나 구조 변경으로 인한 `NoneType` 에러를 방지하기 위해 `try-except` 구문을 적용합니다.
* 수집된 텍스트 중 불필요한 공백, 특수문자, HTML 태그 등을 정제(`strip()`, 정규표현식 활용)합니다.

#### ④ 데이터 저장

* 최종 수집된 데이터를 `pandas DataFrame`으로 변환한 뒤, 추후 활용이 쉽도록 **CSV 파일**이나 **Excel 파일**로 저장합니다.

---

### 💻 3. 파이썬 샘플 코드 구조 (골격)

```python
import time
import requests
from bs4 import BeautifulSoup
import pandas as pd

def crawl_baekban_data():
    base_url = "https://broadcast.tvchosun.com/broadcast/program/3/C201900033/bbs/8667/C201900033_10/list.cstv"
    all_data = []

    # 1페이지부터 18페이지까지 순회
    for page in range(1, 19):
        params = {
            "search_text": "",
            "pg": page
        }
        
        response = requests.get(base_url, params=params)
        if response.status_code != 200:
            print(f"{page}페이지 접속 실패")
            continue
            
        soup = BeautifulSoup(response.text, 'html.parser')
        
        # TODO: 실제 사이트의 HTML 구조에 맞는 CSS Selector로 수정 필요
        posts = soup.select("목록을 담고 있는 태그 셀렉터")
        
        for post in posts:
            # 예시 데이터 추출
            # title = post.select_one("제목 셀렉터").text.strip()
            # link = post.select_one("링크 셀렉터")['href']
            
            # 상세 페이지 접속 및 식당 정보 크롤링 로직 추가 가능
            
            all_data.append({
                "page": page,
                # "title": title,
                # "link": link
            })
            
        print(f"{page}페이지 수집 완료")
        time.sleep(1) # 서버 부하 방지
        
    # 데이터프레임 변환 및 저장
    df = pd.DataFrame(all_data)
    df.to_csv("baekban_episodes.csv", index=False, encoding="utf-8-sig")
    print("모든 데이터 저장 완료!")

if __name__ == "__main__":
    crawl_baekban_data()

```

---

### 💡 4. 개발 시 주의사항

* **동적 로딩 여부 확인:** 만약 방송국 웹사이트가 자바스크립트를 통해 목록을 비동기(AJAX)로 불러오는 방식이라면, `requests` 대신 **Selenium**이나 브라우저 자동화 도구를 사용해야 정확한 데이터를 긁어올 수 있습니다.
* **저작권 및 이용 약관:** 수집한 데이터는 개인적인 분석이나 아카이빙 목적으로만 사용해야 하며, 상업적 재배포 시 법적 문제가 발생할 수 있으니 유의하시기 바랍니다.