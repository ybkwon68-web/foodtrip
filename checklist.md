# 백반기행 웹페이지 구축 체크리스트

## 0단계: 2026-07-30 추가 범위 (식당명/주소 + 지도 링크 + 편집 기능)
- [x] 지역 추정 스크립트: 제목 텍스트 → 사전 매칭으로 지역 후보 추출 (`crawler/regions.py`, 신뢰도 낮음을 "미검수" 배지로 표시)
- [x] 네이버 블로그 검색 기반 식당명/주소/좌표 수집 (`crawler/naver_place.py`, `enrich_naver.py`) — 352건 중 212건(456곳) 확보(오매칭 8건 발견·수정 후 수치), 상세는 context-notes.md 참고
- [x] `supabase/migrations/0001_init.sql` 스키마 작성 — **Supabase 프로젝트 자체는 사용자 계정 필요, 아직 생성 안 됨**
- [x] `api/episodes.js` (GET), `api/episodes/[id].js` (GET/PUT) 서버리스 함수 작성 + mock Supabase로 로직 검증
- [x] `api/auth.js` 관리자 비밀번호 인증 및 토큰 발급 (HMAC 서명, 세션 테이블 없이 상태 없는 검증) + 단위테스트 통과
- [x] `crawler/seed_supabase.py` 최초 데이터 적재 스크립트 작성 — **실행은 Supabase 프로젝트 생성 후**
- [x] 프론트 편집 모드 UI — 이제 실제 `/api/auth`·`/api/episodes/:id` PUT을 호출하도록 연결 (API 없는 로컬 환경에서는 정적 JSON 폴백 + 로그인 실패 메시지)
- [x] 네이버맵 링크 생성 로직 — placeId 있으면 정확한 장소 페이지로, 없으면 `식당명 + 주소` 검색으로 연결
- [x] 편집 API rate limit — Upstash Redis(REST API)로 IP당 10분에 5회 제한 구현(`lib/rateLimit.js`), 프로덕션에서 6회 연속 로그인 시도로 5회까지 정상·6회째 429 확인

## 1단계: 크롤러
- [x] `crawler/crawl.py`에 목록 페이지 파싱 로직 작성 (셀렉터: `ul.item-list.col-4.wrap > li > a.vd-link`)
- [x] 1페이지만 실행해 결과 검증 (20개 항목, 제목/날짜/링크/썸네일 정상 추출 확인)
- [x] 18페이지 전체 목록 수집 실행 및 항목 수 검증 → 352건, 결번/중복 없음
- [x] 상세페이지 파싱 로직 작성 (셀렉터: `.board-view .cont-box`)
- [x] 상세페이지 샘플 몇 건 실행해 본문 HTML 정상 추출 확인
- [x] 전체 상세페이지 크롤링 실행 (352건, 실패 0건)
- [x] 회차번호 정규식 추출 검증 (실패 0건)
- [x] `seed/episodes.json` 생성 및 데이터 정합성 확인 (1~352회 결번·중복 없음 확인)

## 2단계: 웹페이지
- [x] `public/index.html` 기본 레이아웃 작성
- [x] `script.js`: episodes.json fetch 및 목록 카드 렌더링
- [x] 검색 기능 (제목·지역 텍스트 필터)
- [x] 정렬 기능 (최신순/회차순)
- [x] 상세 뷰 렌더링 (URL 해시 라우팅, 본문 HTML 삽입 + DOMPurify sanitize)
- [x] `style.css` 반응형 카드 레이아웃 (모바일/데스크톱)
- [x] 이미지 로드 정상 여부 확인 → 핫링크 차단 없음 확인, 다만 http 이미지 URL을 https로 강제 변환 필요해 크롤러에 반영함(mixed content 방지)

## 3단계: 로컬 테스트 (Playwright로 실제 확인)
- [x] 목록 전체 렌더링/카드 클릭 → 상세 이동 골든패스 확인
- [x] 검색 결과 없음 케이스 확인
- [x] 검색 결과 있음(부산 7건) 케이스 확인
- [x] 편집 모드 토글, 상세 화면 인라인 편집 폼 열기/닫기 확인
- [x] 모바일 화면 크기(390px)에서 레이아웃 확인 — 카드 2열, 헤더 컨트롤 줄바꿈 정상

## 3-1단계: 회차별 요약표 페이지 (2026-07-30 추가)
- [x] `public/table.html` + `table.js`: 회차/식당명/소개된메뉴/한줄평/도·시군구/상세주소 표 (별도 페이지, 카드 화면과 상호 링크)
- [x] 소개된메뉴·한줄평은 4개 subagent가 방송 본문을 직접 읽어 작성 (220개 회차, 최종 456개 식당분)
- [x] 네이버 매칭 오류 발견 및 수정 — 8개 그룹 13개 회차가 다른 회차와 동일한 식당 세트를 공유하던 버그 확인, 8개 회차는 전체 초기화·2개 회차는 항목 일부만 제거. `naver_place.py`에 지역 교차검증 추가해 재발 방지
- [x] Playwright로 표 페이지, 오류 수정 반영 재검증

## 4단계: 배포
- [x] `git init` 및 최초 커밋
- [x] GitHub 원격 저장소 push (`ybkwon68-web/foodtrip`, main 브랜치)
- [x] Vercel 프로젝트 연결 (`ybkwon68-5258s-projects/foodtrip`, `vercel link`)
- [x] 배포된 URL에서 최종 동작 확인 — 프로덕션(`https://foodtrip-six.vercel.app`)에서 목록/episodes API/로그인/인증거부/PUT저장/원상복구 curl로 전부 확인

## 5단계: Supabase 연동 (사용자 계정 필요, 다음 단계)
- [x] Supabase 프로젝트 생성 (`acshmogxwmoluuvjcytn`)
- [x] `supabase/migrations/0001_init.sql` 실행 (SQL 에디터에 붙여넣기)
- [x] `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` 확인 → `.env.local`에 저장(gitignore 대상, 커밋 안 됨)
- [x] `ADMIN_PASSWORD`(원하는 편집 비밀번호), `SESSION_SECRET`(임의의 긴 문자열) 정하기
- [x] `crawler/seed_supabase.py` 실행해 초기 데이터 적재 — 352/352건 성공, REST API로 건수 재확인 완료
- [x] 로컬에서 `/api/auth`·`/api/episodes`·`/api/episodes/[id]` 핸들러를 실제 프로덕션 Supabase에 직접 호출해 로그인/인증거부/목록조회/PUT수정/원상복구 종단 간 검증 (7단계 전부 통과)
- [x] Vercel 프로젝트에 위 4개 환경변수 등록 (Production/Preview/Development 3개 환경 모두)
- [x] 배포된 실제 URL(curl)에서 로그인 → 식당 정보 수정 → 저장 반영 최종 확인

## 6단계: 2026-07-31 데이터 보강 + 지도 페이지 추가
- [x] 서브에이전트 오작동(임의 커밋/푸시/파일 오염) 수습 및 데이터 정합성 복구
- [x] 네이버 블로그 자동 재검색 + 오매칭 방지 로직 3종 추가(부분 문자열 오매칭, 키워드 교차검증, 식당 place_id 전역 중복 방지)
- [x] 남은 미확보 회차 직접 조사(웹검색) — 240→270/352(77%) 확보, 근거 부족한 건 비워둠
- [x] 미확보 82개 회차 목록 CSV 생성(`미확보_회차_목록.csv`, 수작업 조사용)
- [x] 헤더 UI 정리(제작자 표시, 네이버맵 링크 문구 축소), 상세화면 상세주소 노출
- [x] 요약표에 도/시군구 다중선택 필터(엑셀 자동필터 스타일, 상호 종속) 추가
- [x] **지도로 보기 페이지 추가** — 네이버 지도 API(Client ID 발급받음)로 좌표 있는 식당 전부 마커 표시, 확대/축소, 검색 필터, 마커 클릭 시 인포윈도우(회차·주소·링크). 해외 특집 회차 좌표 때문에 초기 화면이 과도하게 축소되는 문제 수정(국내 좌표 기준으로 fitBounds)
- [x] 사용자가 라이브 사이트에서 직접 편집한 260·352회 데이터를 재적재 전 로컬과 동기화(데이터 유실 방지) → Supabase 재적재 완료, 프로덕션 270/352 확인

## 7단계: 2026-07-31 PC 이전 + 라이브 편집 반영 버그 수정
- [x] PC 이전 후 로컬 개발 환경 복구 (`.env.local` Vercel에서 pull, `npm install`, `vercel link`)
- [x] 표로 보기가 편집 화면 저장 내용을 반영하지 않던 버그 수정 (`table.js`가 정적 스냅샷 대신 `/api/episodes` 우선 조회하도록 변경)
- [x] 지도로 보기에 신규/주소변경 식당 마커가 안 뜨는 문제 해결 — NCP Geocoding 대신 Kakao Local API로 전환하여 좌표 자동조회 연동 완료
- [x] 표로 보기 수정사항 및 지오코딩 API 변경본 프로덕션 배포 완료 (Vercel GitHub 자동 배포 연동 확인)
- [x] 지도로 보기 마커 클러스터링 도입 (네이버 공식 `MarkerClustering.js` 자체 호스팅, 밀집 지역 가독성·성능 개선)

## 8단계: 2026-08-02 1~150회 식당정보 블로그 3편 대조 보강
- [x] tineas 블로그 백반기행 1~50/51~100/101~150회 정리글 3편 파싱 (회차·식당명·주소·소개음식, 706건)
- [x] 식당정보가 아예 없던 42개 회차(209곳) 신규 등록 — 주소 정규화(지번 제거·시도명 공식화) 후 프로덕션 API로 저장, 카카오 지오코딩 자동 실행
- [x] 기존 108개 회차 대상 이름·주소 대조로 누락 식당만 96개 회차·323곳 추가 등록(기존 항목·verified 상태는 유지), 완전 동일 식당으로 판단된 5건은 표기 차이로 보고 제외
- [x] 지오코딩 실패한 11곳(1차 3곳, 2차 8곳)을 사용자가 확인한 정확한 주소로 재저장해 좌표 확보
- [x] 결과: 1~150회 전 구간 식당정보 확보 완료(150/150), 전체 352회 중 341회에 식당정보 존재(총 1,129곳)
- [x] `미확보_회차_목록.csv` 53→11건으로 감소(남은 11건은 151회 이후, 블로그 대조 대상 밖)
- [x] 사용자 제보로 231회(pinkbong.tistory.com)·227회(blog.naver.com/ksypr) 추가 근거 확보 → 6곳 반영(231회 거제도굴구이는 지리적으로 무관해 제외), `미확보_회차_목록.csv` 11→8건
- [x] 147회 "반영 누락 의심" 확인 요청 → 실제로는 블로그 4곳 전부 이미 등록돼 있었음(소개메뉴만 비어있던 것), 사용자가 "기존 등록 식당은 손대지 않는다" 원칙 재확인
- [x] 1~150회 전체를 완화된 기준(이름 2자 이상 부분일치 OR 주소 일치)으로 블로그 706건과 프로덕션 DB 재대조 → 누락 0건 확인

## 9단계: 2026-08-04 add 페이지 자동조사 버그로 인한 데이터 손상 정리
- [x] add 페이지 자동조사(자동탐색) 매칭 버그로 무관한 식당이 저장된 사례 전수 점검(139·157·165·168·198·115·230·91·78·70·64·51회)
- [x] 완전히 무관한 데이터는 미확보 상태로 초기화, 139회는 로컬 스냅샷에 남아있던 원본으로 복구
- [x] 정상 식당 사이에 1곳만 잘못 섞인 케이스(91·78·70·64·51회)는 그 항목만 제거
- [x] region 태그 오류 점검 — 53회("장흥"→실제 경기 양주시 장흥면 동명이인) 수정, 106회("하동-광양" 복합지역)는 필드 구조상 한쪽으로 바꿔도 개선 안 돼 유지
- [x] `미확보_회차_목록.csv` 최신 상태로 갱신

## 10단계: 2026-08-04 자연어 식당 추천 기능
- [x] Gemini API 기반 자연어 추천 설계 — 1차 호출로 출발지·이동시간 추출 → 카카오 지오코딩+직선거리(분×0.8km) 반경 필터 → 2차 호출로 후보 중 2~3곳 선정+추천사유 생성
- [x] `lib/recommend.js`(거리계산·후보목록·응답파싱 순수로직, 단위테스트), `lib/gemini.js`(API 호출), `api/recommend.js`(엔드포인트, IP당 10분 8회 rate limit), 프론트 `public/recommend.js` + 헤더 아래 프롬프트 UI
- [x] `vercel dev`로 로컬 종단 간 테스트 — 이 PC의 OS 환경변수에 이미 있던 GEMINI_API_KEY를 자동으로 물려받아 실제 추천 확인(과천·조용한 술자리 조건 → 성남/서초 3곳, 거리·이유 모두 적절)
- [x] GEMINI_API_KEY를 Vercel 프로젝트 환경변수(Production/Preview/Development)에 등록, `.env.local`에도 추가
- [x] 결과 카드 위 "결과 닫기" 버튼 추가(닫으면 결과 초기화+입력창 비움), 입력창 스크롤바 제거(입력한 만큼 자동으로 높이 증가)
- [x] Gemini 무료 등급 일일 호출 한도(gemini-2.5-flash 기준 20회/일) 소진 확인 → 요청당 Gemini 호출을 2회(조건 추출+최종 선정)에서 1회로 축소: 출발지·이동시간 추출을 `lib/koreanRegions.js`(crawler/regions.py 사전 이식) + 정규식 기반 로컬 처리로 전환, 최종 후보 선정만 Gemini 호출. 실질 일일 처리량 10건→20건으로 2배
- [x] 할당량 초과(429) 에러 메시지를 사용자 친화적 문구로 교체("죄송합니다. 토큰 사용량이 한도초과 되었으니 잠시 쉬셨다가 다시 시도해 주세요!")
- [x] "다른 곳으로 다시 추천받기" 버튼 추가 — 같은 요청 문장(prompt)을 그대로 유지한 채, 방금 보여준 식당들을 `exclude`로 서버에 전달해 후보에서 제외하고 재추천(`lib/recommend.js`의 `excludeCandidates`, 단위테스트 포함)
- [ ] 오늘 소진된 무료 할당량이 초기화되면 1회 호출 구조 + "다시 추천받기" exclude 흐름까지 실제 Gemini 응답으로 종단 간 재확인 필요(단위테스트·요청 페이로드는 확인했으나 할당량 소진으로 실제 응답 검증은 못함)

## 11단계: 2026-08-04 add 페이지(+추가등록) 오매칭 방지 로직 점검·보완
- [x] `+추가등록` 페이지의 자동조사 파이프라인(`public/add.js`→`api/lookup.js`→`lib/lookup.js`) 점검 — 오늘 데이터 손상(139·157·168·198회 등)의 실제 원인이 네이버 블로그 검색에 오매칭 방지 로직이 전혀 없었기 때문임을 확인. `crawler/naver_place.py`(오프라인 크롤러)엔 이미 있던 안전장치 3종이 웹 add 페이지엔 이식돼 있지 않았음
- [x] `lib/lookup.js`에 제목/회차 일치 검증(`titleMatchesEpisode`) 추가 — "백반기행" 미언급 글은 무조건 거절, 회차번호는 정규식으로 전체 숫자 비교("44회"가 "344회"에 부분일치 안 되게), 번호가 맞아도 방송 제목 키워드가 하나도 안 겹치면 거절
- [x] `lib/lookup.js`에 지역 교차검증(`regionConflicts`) 추가 — 찾아낸 식당 주소의 시/도가 예상 지역과 전부 다르면 오매칭으로 간주(139/157회 사고의 직접 원인)
- [x] `api/lookup.js`에 전역 중복 방지 추가 — Supabase 전체 조회로 이미 다른 회차가 채택한 블로그 글(source_url)·식당(place_id)을 제외 목록으로 만들어 재사용 차단(139/157회가 완전히 같은 식당 6곳을 공유했던 사고의 직접 원인)
- [x] 단위테스트 10개 추가(`test/lookup.test.js`) — 오늘 실제 발생한 168/139/157회 사고를 각각 재현하는 회귀 테스트 포함
- [x] `vercel dev`로 실제 네트워크 호출까지 포함한 종단 간 검증 — 139회(현재 미확보 상태) 검색 시 예전엔 무관한 식당이 나왔지만 이제 `restaurants: []`로 안전하게 거절됨을 확인, 348회(정상 케이스) 검색 시 경기도 고양시 식당 3곳이 정상적으로 나옴을 확인해 안전장치가 정상 매칭까지 막지 않음을 검증
- [x] `searchTvChosun` 숫자 회차 검색의 fuzzy substring 폴백 제거 — 회차번호가 있으면 정확히 일치하는 것만 신뢰하고, 안 맞으면 다음 페이지로(다른 회차의 제목·날짜에 숫자가 우연히 포함돼도 더 이상 오매칭 안 됨)
- [x] `extractNaverBlogPlaces`가 블로그 페이지 하나의 og:description을 그 글에서 찾은 모든 식당의 review에 동일하게 넣던 문제 수정 — review는 이제 자동으로 채우지 않고 null로 두어 사람이 방송 본문을 보고 직접 채우게 함(139/157회 사고의 "오늘 방송된 백반기행의 제목입니다" 재발 방지), 페이지 요약 자체는 `description` 필드로 계속 반환
- [x] 자동 수집 결과 미리보기가 첫 번째 식당만 보여주던 문제 수정 — 이제 발견된 식당 전체를 번호 매겨 나열하고 "지역이 맞는지 꼭 확인하세요" 안내 추가
- [x] 단위테스트 2개 추가 + `vercel dev`로 348회 실제 검색 후 미리보기에 3곳 전부 표시되는 것을 스크린샷으로 확인
- [x] `public/add_helpers.js`(add.html에서 로드되지 않던 `renderReviewSummary` 중복 구현, 죽은 코드) 삭제. 테스트 전체 재실행해 영향 없음 확인

## 12단계: 2026-08-04 자연어 추천 — 막연한 요청에 되묻기 기능 추가
- [x] 문제: "배고파 밥먹고 싶어"처럼 위치·음식종류·분위기 중 아무 단서도 없는 요청에도 후보 80개 중 아무거나 3곳을 뽑아 그럴듯한 이유를 지어내던 문제 확인
- [x] Gemini 호출 횟수를 늘리지 않고 기존 1회 호출(`pickRecommendations`) 안에서 해결 — 모델이 먼저 요청의 구체성(지역/이동거리, 음식/메뉴, 분위기·목적 중 최소 하나)을 판단해 `{"type":"clarify","question":"..."}` 또는 `{"type":"picks","items":[...]}` 중 하나로 응답하도록 프롬프트·응답 스키마 변경
- [x] `lib/recommend.js`의 `parsePicksResponse`를 `parseRecommendResponse`로 개편(clarify/picks 분기 파싱, 하위호환으로 배열만 오는 경우도 처리), 단위테스트 5개 갱신/추가
- [x] 되묻기는 **1회로 제한** — 프론트(`public/recommend.js`)가 되묻기 질문을 받으면 원래 질문을 기억해뒀다가, 사용자가 답변을 입력하면 "원래 질문 + 답변"을 이어붙여 `forcePicks:true`로 재요청(서버 상태 저장 없이 프론트에서 문자열만 이어붙임). `forcePicks`가 오면 프롬프트에서 clarify 옵션 자체를 제거해 모델이 무조건 추천하도록 강제. 모델이 그래도 다시 되물으면(지시 불이행 대비) 안내만 보여주고 루프 없이 종료하는 방어 로직 추가
- [x] `api/recommend.js`에 `forcePicks` 파싱 및 `needsClarification` 응답 처리 추가, CSS에 되묻기 질문 전용 스타일 추가
- [x] 실제 curl 호출로 "배고파 밥먹고 싶어" → `needsClarification: true`와 자연스러운 되묻기 질문 확인(무료 할당량 문제로 브라우저 종단 간 답변 이어붙이기 흐름까지는 재검증 못함 — 코드 로직과 단위테스트로만 확인된 상태)
- [x] 2026-08-05: 사용자가 새 `GEMINI_API_KEY` 제공, `vercel dev`(포트 8000) + Node fetch로 "배고파 밥먹고 싶어" → clarify 확인 → 답변 이어붙여 forcePicks 재요청 → picks 3곳 정상 수신까지 전체 흐름 실제 검증 완료(더 이상 재확인 불필요)

## 13단계: 2026-08-05 추천받기 후보 선별 로직 개선
- [x] 위치조건 없는 질의(음식종류·분위기 중심)에서 `buildCandidateList`가 verified 우선 정렬만 쓰던 문제 확인 — 전체 1,135곳 중 verified는 99곳뿐이라 나머지는 회차순으로 대충 80개까지만 채워져 Gemini가 실제로 못 보는 후보가 태반이었음
- [x] `lib/recommend.js`에 `extractRelevanceKeywords`(불용어/조사 제외 키워드 추출)와 `relevanceScore`(name/menu/review/region/title 매칭) 추가, `buildCandidateList`가 origin 없을 때 관련도 점수(동점이면 verified) 순으로 정렬하도록 변경. 키워드가 없으면 기존 verified 우선 동작 그대로 유지(하위호환)
- [x] `api/recommend.js`에서 `buildCandidateList` 호출 시 `query` 전달, 단위테스트 4개 추가(관련도 우선순위, 키워드 없을 때 폴백, 키워드 추출) — `npm test` 전체 스위트 재실행해 기존 테스트 포함 전부 통과 확인
- [x] review 텍스트 80자 절단 문제로 지목했던 항목 재검토 — 실데이터(505건) 확인 결과 review 최대 길이가 53자로 80자 한도에 전혀 걸리지 않음(오탐), 코드 변경 불필요로 결론
- [x] GPS 기반 "현재 위치" 옵션 추가 — `public/index.html`에 "📍 내 위치" 토글 버튼과 상태 안내 문구 추가, `public/recommend.js`가 `navigator.geolocation`으로 좌표를 얻어 요청 본문에 `originCoords`로 실어 보냄(거부/미지원 시 안내 문구 표시), `api/recommend.js`가 `originCoords`가 있으면 지명 지오코딩을 건너뛰고 그 좌표를 바로 출발지로 사용
- [x] GPS 기능 구현 중 발견한 연관 버그 수정 — 출발지는 있지만("과천인데") "N시간/N분" 같은 이동시간 표현이 없으면 반경 필터가 아예 적용 안 되던 문제(`radiusKm`가 null). `lib/recommend.js`에 `resolveRadiusMinutes`(기본 60분) 추가해 출발지가 있으면 항상 반경 필터가 적용되도록 수정, 단위테스트 추가
- [x] `node --check`로 변경 파일 문법 검증 + 로컬 정적 서버로 새 DOM 요소(`recommendLocationBtn` 등)·CSS·JS가 정상 서빙되는지 확인
- [x] 사용자가 `GEMINI_API_KEY` 제공(`.env.local`에 추가) → `vercel dev`(포트 8000)로 `/api/recommend` 5개 시나리오 실제 호출 검증: (1) 막연한 질의 → clarify, (2) forcePicks 재요청 → picks, (3) 음식종류만 있는 질의(위치조건 없음) → 관련도 기반 선별 확인(해산물 질의 → 실제 해산물 전문점 3곳), (4) 위치+시간표현 없음 → 기본반경(48km) 적용 확인, (5) GPS 좌표 → origin: "현재 위치" 확인. exclude(다시 추천받기) 흐름도 별도 확인, 제외 목록과 겹치지 않는 새 후보 반환됨
- [ ] GPS "내 위치" 버튼의 실제 클릭·브라우저 위치권한 프롬프트 동작은 이 환경에 브라우저 자동화 도구가 없어 검증 못함 — 사용자가 직접 브라우저에서 확인 필요(API 자체는 위 항목에서 GPS 좌표로 검증 완료, 프론트 버튼 클릭 흐름만 미검증)

## 14단계: 2026-08-05 사용자 실사용 중 발견 — 출발지 오인식/GPS 우선순위 버그 수정
- [x] 사용자가 배포 사이트에서 "내 위치"(과천)를 누르고 "친구들과... 친구들은 위례, 서울 강남이 집이라 셋이 모이기 편한곳으로" 입력 → 결과가 전부 강남 근처만 나옴을 리포트. GPS가 실제로는 실패(권한 거부 등으로 추정, 화면에 실패 안내 문구가 떠 있었음)했고, 텍스트에서 "강남"(친구 거주지 설명)을 사용자 본인 출발지로 오인식한 게 원인임을 재현으로 확인
- [x] 관련 케이스 추가 점검 요청 — "GPS(과천)로 위치를 찍은 뒤 '지금 광주인데, 맛있는집 추천해줘'라고 텍스트에 다른 위치를 명시하면?" → 기존 코드는 GPS가 있으면 텍스트를 통째로 무시(광주 언급이 조용히 버려짐)했음을 코드 추적으로 확인
- [x] 사용자가 우선순위 확정: 1인칭으로 명시된 텍스트 위치("지금 나는/저는 ~") > GPS > 그 외 텍스트 추정 위치. `lib/koreanRegions.js`에 위치 정보 포함 매칭 함수(`findAllRegionMatches`) 추가(기존 `findRegionInText`는 그대로 유지, `api/lookup.js` 등 다른 사용처에 영향 없음)
- [x] `lib/recommend.js`에 `extractOwnOrigin` 추가 — 지명 바로 앞 절에서 1인칭 표현("나는/저는/난/내가/제가/지금")과 3인칭 표현("친구는/가족은/애들은" 등, 주어+조사)의 등장 위치를 비교해 어느 쪽이 지명에 더 가까운지로 문맥 판단. 1인칭이 있으면 explicit:true로 최우선 채택, 3인칭만 있으면 후보에서 제외, 남는 게 없으면 origin:null(모름) 반환 — "강남 오인식"과 "GPS 무시" 두 문제를 같은 로직으로 해결
- [x] `api/recommend.js` 출발지 결정 순서를 `intent.originExplicit`(1인칭 텍스트) → GPS → 그 외 텍스트 순으로 재배치
- [x] 단위테스트 5개 추가(1인칭 명시, 3인칭 제외 재현, 1인칭+3인칭 혼재 시 1인칭만 채택, 화자 표현 없을 때 기존 규칙 폴백, `extractIntentLocal` 반환 형태 변경분), 전체 스위트 22개(lib/recommend.js) 통과
- [x] `vercel dev`로 3개 시나리오 실제 검증: (1) 강남 오인식 재현 문장(GPS 없음) → `origin: null`로 안전하게 처리, 결과가 강남에 쏠리지 않고 서울 각지에 분산됨 (2) GPS(과천)+텍스트("지금 광주인데") 충돌 → `origin: "광주광역시"`(텍스트 우선), 결과 전부 실제 광주 소재 (3) GPS만 있고 텍스트에 위치 없음 → 여전히 `origin: "현재 위치"`(GPS) 정상 사용
- [x] 사용자 지적: "광주"는 광주광역시/경기도 광주시 동명이지역인데 하나로 확정해버리면 안 되고 헷갈리면 되물어야 함. `lib/koreanRegions.js`에 사전을 스캔해 "{시도} {짧은이름}" 형태 조합 키(예: "경기도 광주")가 짧은이름 단독 키와 다른 지역을 가리키면 자동으로 동명이지역으로 판정하는 `ambiguousRegionsFor` 추가(하드코딩 목록이 아니라 사전에서 자동 도출 — 현재 "광주"/"고성" 2건 해당, 향후 사전에 같은 패턴이 추가돼도 자동 적용됨). "남양주"처럼 긴 지명이 짧은 지명을 문자열로 포함하는 것뿐인 경우(공백 없는 키)는 오탐 없이 제외됨을 확인
- [x] `lib/recommend.js`의 `extractOwnOrigin`이 채택한 지명이 동명이지역이면 origin을 확정하지 않고 `ambiguous:{name, options}`를 반환하도록 수정. `api/recommend.js`가 이 신호를 받으면 Gemini 호출·DB 조회 전에 바로 `needsClarification`으로 되물음("경기도 광주" 등 구체적으로 답하면 다음 요청에서 바로 해결). `forcePicks`(되묻기에 이미 답함) 요청은 `allowAmbiguous:true`로 넘겨 무한 되묻기 없이 기본 해석(광주→광주광역시)으로 확정
- [x] 단위테스트 4개 추가(동명이지역 되묻기 신호, 구체적으로 쓰면 바로 확정, allowAmbiguous 폴백, 기존 1인칭 테스트를 비-동명이지역 예시로 교체), `vercel dev`로 실제 검증: "지금 광주인데" → clarify 응답(Gemini 호출 안 함, DB 조회도 안 함) → "경기도 광주요" 답변 이어붙여 forcePicks 재요청 → 경기도 광주시 식당들 정상 추천, 처음부터 "지금 경기도 광주인데"로 물으면 되묻지 않고 바로 추천됨을 확인
- [x] 사용자가 배포 사이트에서 실제로 되묻기 질문을 받았는데 "답할 창이 없다"고 리포트 — 기존엔 위쪽 검색창을 비워서 재사용하도록 했는데, 빈 칸에 긴 placeholder 예시문구만 보여서 입력 가능한 칸처럼 안 보였던 게 원인으로 추정됨. `public/recommend.js`의 `renderClarifyQuestion`이 질문 아래에 전용 답변 입력창(`recommendClarifyForm`+`recommendClarifyInput`)을 만들도록 변경, `recommendResult`에 `submit` 이벤트 위임 리스너 추가. 위쪽 검색창으로도 여전히 답변 가능(보조 경로로 유지). 부수적으로 값을 비울 때 자동증가했던 textarea 높이도 같이 초기화하도록 수정(`resetRecommendInput`)
- [x] 위 입력창으로 "광주" 되묻기에 "경기도 광주야"라고 답했더니 "조건에 맞는 식당을 찾지 못했습니다"가 나온다는 리포트 → 재현해보니 `${lastQuery} ${typed}`처럼 마침표 없이 그냥 이어붙여서, 답변("경기도 광주야")이 원래 문장 끝의 "친구들은 위례와 서울 강남이..."와 같은 절로 묶여 3인칭 문맥으로 오인되고 있었음(originExplicit/동명이지역 로직과는 별개로, 문장 절 구분 자체가 안 된 문제). `submitClarifyAnswer`가 `${lastQuery}. ${typed}`처럼 마침표로 끊어 이어붙이도록 수정 — 재발 방지용 단위테스트 1개 추가(`lib/recommend.js` 26개 통과), `vercel dev`로 정확히 같은 문장+답변을 실제 호출해 "경기도 광주시" 식당들이 정상 추천됨을 확인
- [x] 위 수정 반영 후 실제 결과 확인 — "경기도 광주시" 기준 반경필터로 성남 지역 식당들이 나옴, 우연히 위례·성남과 가까워 그럴듯해 보였지만 실제로는 세 사람(광주·위례·강남) 위치를 종합 계산한 게 아니라 "1인칭 위치 1곳 기준 반경필터"일 뿐이라는 한계를 사용자에게 설명함
- [ ] **(다음 세션 이후 과제로 등록, 지금은 착수 안 함)** 여러 사람의 출발지를 종합해 중간지점을 찾는 기능. 사용자가 "일단 추천기능은 여기까지"로 이번 세션 범위를 마무리하며 이 기능은 나중으로 미룸. 구현하게 되면: 문장에서 여러 사람의 위치를 각각 추출(현재 `extractOwnOrigin`은 "내 위치" 1곳만 판별하도록 설계돼 있어 다자 추출로 확장 필요) → 각각 지오코딩 → 무게중심(또는 최대거리 최소화 지점) 계산 → 그 지점 기준으로 후보 필터링, 순서로 예상됨


## 15단계: 2026-08-05 모바일 헤더 레이아웃 축소
- [x] 사용자 리포트 — 모바일에서 헤더(검색/최신순/표로보기/지도로보기/추가등록/편집모드)가 각각 세로로 풀폭 스택되어 실제 콘텐츠 보이는 영역이 너무 작음
- [x] `public/style.css`의 `@media (max-width: 720px)` 블록 수정 — 검색창은 그대로 풀폭 유지하되, 정렬/표로보기/지도로보기는 세로 스택 대신 한 줄에 나란히 배치(`flex:1`, 높이·패딩·폰트 축소). `table.html`/`map.html`도 같은 CSS 클래스를 공유해 자동으로 함께 컴팩트해짐
- [x] `#addEntryLink`(추가등록)·`#editToggle`(편집모드)를 모바일에서 `display:none`으로 완전히 숨김(로그인/편집모드 상태와 무관하게 항상 숨김)
- [x] Playwright(`npx playwright screenshot`)로 390×844 모바일 뷰포트 실제 렌더링 캡처해 확인 — 헤더가 검색창 1줄+정렬/표/지도 1줄로 축소됨, 추가등록·편집모드 완전히 안 보임을 확인
- [ ] 확인 중 "지도로 보기" 링크에 밑줄이 보이는 기존 스타일 이슈 발견(`.edit-toggle`에 `text-decoration:none`이 원래 없음) — 이번 작업 범위 밖이라 손대지 않음, 필요하면 별도로 정리

## 16단계: 2026-08-05 지도로 보기 — "내 위치로 보기" 버튼 추가
- [x] `public/map.html`에 `#map`을 감싸는 `.map-wrap` 추가하고 지도 오른쪽 아래에 `#mapLocateBtn`(📍) 버튼 배치(줌 컨트롤 TOP_RIGHT·네이버 로고와 안 겹치는 위치)
- [x] `public/map.js`의 `locateMe()` — 브라우저 위치 권한 요청 → 성공하면 그 위치로 지도 이동(줌 15) + 파란 점 마커 표시(재클릭 시 마커 위치만 갱신, 중복 생성 안 함). 이 마커는 식당 클러스터링 대상(`markers` 배열)과 분리된 별도 `naver.maps.Marker`라 검색 필터링에 영향 없음. 거부/미지원 시 안내 문구 표시(recommend 페이지 "내 위치" 버튼과 동일 패턴)
- [x] `public/style.css`에 `.map-locate-btn`(원형 플로팅 버튼)·`.map-locate-status`·`.map-my-location-dot` 스타일 추가
- [x] Playwright로 데스크톱(1280×720)·모바일(390×844) 뷰포트 둘 다 `context.grantPermissions(['geolocation'])`+가짜 좌표(과천)로 실제 클릭까지 검증 — 지도가 정확히 과천시청 근처로 이동하고 마커가 찍힘, 버튼 위치가 줌 컨트롤·네이버 로고와 안 겹침을 스크린샷으로 확인
- [x] `vercel dev`(포트 8000)로 로컬 서버 띄워 사용자가 직접 브라우저에서 확인 완료

## 17단계: 2026-08-05 전체 데이터 품질 점검 (사용자 수동 편집 이후)
- [x] 사용자가 일부 데이터를 직접 편집 입력한 뒤 전체 점검 요청 — 8가지 관점(시/도 화이트리스트, region-주소 불일치, 좌표 누락, 지어낸 값 의심, 빈 이름, place_id 중복, source_url 중복, 이름+주소 중복)으로 352회차 전수 스캔 스크립트 작성(읽기 전용, 로컬 vercel dev로 실제 운영 Supabase 조회)
- [x] 253회 경도회관 주소가 표에서 안 갈라지던 문제 확인 — DB의 실제 주소는 이미 "전라남도 여수시 대경도길 2-2"로 정상(사용자가 직접 고침), `splitAddress` 로직 자체는 문제없음을 확인
- [x] 좌표 없는 식당 17곳 발견 → `api/episodes/[id].js`의 기존 자동 지오코딩 경로(PUT 시 좌표 없는 항목만 지오코딩)를 그대로 재사용해 GET→PUT으로 15곳 자동 해결. 나머지 2곳(208회 이로운밥상, 251회 손칼국수)은 주소 자체가 불완전/오타로 지오코딩 실패 → 사용자가 정확한 주소 제공 → 수정 후 지오코딩 성공
- [x] 회차 region과 식당 주소 시/도 불일치 4건 발견, 사용자 확인: 106회(하동-광양, 기존에 알려진 복합지역 케이스)·88회(천안 청룡원조매운탕, 정상 방송분) 문제없음. 50회(고기리막국수, 송파 밥상인데 용인 소재)는 사용자가 미확인 상태로 직접 조사 중, 보류(삭제하지 않음)
- [x] 같은 place_id/이름+주소가 다른 회차에 중복 등록된 3쌍 발견, 사용자 확인 후 처리: 잡어와묵은지(312회 방송 확정 → 35회 쪽 항목 제거, 같은 회차 다른 7곳은 그대로 유지), 수안보대장군(322회 방송 확정 → 63회 쪽 항목 제거, 같은 회차 다른 5곳은 그대로 유지), 나드리식품(132·30회 둘 다 방송 확정 → 둘 다 유지)
- [x] 157회 restaurants가 이미 빈 배열인데 restaurants_source_url만 예전(11단계) 사고의 잔재로 남아있던 것 발견 — `api/episodes/[id].js`의 PUT이 이 필드를 다루지 않아 Supabase에 직접 연결(서비스롤 키)해 null로 정리. 다른 필드/회차는 손대지 않음
- [x] 해외 특집 24건(캐나다·홍콩·일본, 278·279·283·284·301·302·329·330회)은 시/도 화이트리스트 예외로 정상 설계된 동작임을 재확인, 조치 없음
- [x] 재점검 결과 좌표 누락 0건, place_id/source_url 중복 0건, 이름+주소 중복 1건(나드리식품, 확인된 정상 케이스)만 남음 — 실질적으로 발견된 문제 전부 해결

## 18단계: 2026-08-06 자연어 추천 — 여러 사람 위치 종합 중간지점 찾기
- [x] 17단계 세션에서 "다음 과제"로 등록만 해두고 미착수 상태였던 기능. 사용자에게 입력 방식(자유 텍스트 문장 확장 vs 위치 각각 명시 입력 UI)을 먼저 확인 → 자연어 문장 확장 방식으로 진행 결정
- [x] `lib/koreanRegions.js`의 기존 `findAllRegionMatches`(위치 정보 포함 지명 매칭)를 재사용해 `lib/recommend.js`에 `extractMeetupOrigins` 추가 — "중간지점/모이/만나/가운데" 등 만남 표현이 문장에 있고, 서로 다른 지명이 2곳 이상 언급된 경우에만 동작(단순히 지명이 여러 개 나온다고 자동으로 여러 명 모임으로 해석하지 않음 — 키워드+지명 2곳 이상 이중 조건으로 오작동 방지). 1인칭/3인칭 구분 없이 언급된 지명을 전부 참가자 위치로 모음(기존 `extractOwnOrigin`의 "1인칭만 채택, 3인칭 제외" 로직과는 별개 함수 — 단일 출발지 흐름은 전혀 건드리지 않음)
- [x] `computeCentroid`(위경도 단순 평균) 추가 — 한국 내 지역 간 거리 범위에서는 실용적으로 충분한 근사치로 판단, 가중치·기하 중앙값 등은 적용하지 않음(단순함 우선)
- [x] `api/recommend.js`에 통합 — meetup이 감지되면 참가자 지명을 전부 지오코딩 후 무게중심을 출발지로, "무게중심→각 참가자" 최대거리를 반경으로 사용(참가자들이 멀리 흩어져 있으면 반경도 넓어짐). 일부 지오코딩 실패 시에도 나머지로 계속 진행, 전부 실패하면 지역조건 없이 추천. "광주"처럼 동명이지역이 섞여 있으면 기존 단일 출발지 흐름과 동일하게 되묻기(1회 제한) 적용
- [x] 성공/부분실패 케이스 모두 `notice` 필드로 "○○·○○ 중간지점 기준으로 찾았습니다" 같은 안내문 생성 — 프론트(`public/recommend.js`)는 별도 UI 변경 없이 기존 notice 렌더링 경로를 그대로 활용(과설계 방지)
- [x] 단위테스트 6개 추가(`test/recommend.test.js`) — computeCentroid, 키워드/지명 2곳 미만이면 동작 안 함, 1·3인칭 구분 없이 전부 수집, 동명이지역 되묻기, allowAmbiguous 확정, **되묻기 답변을 원문 뒤에 이어붙였을 때 원문의 애매한 짧은 지명("광주")과 답변의 구체적 지명("경기도 광주")이 중복 참가자로 잡히던 실제 버그**를 재현하는 회귀테스트. `npm test` 전체 32개(lib/recommend.js) 통과
- [x] `vercel dev`(포트 8000)로 실제 Gemini 호출까지 포함한 3개 시나리오 검증: (1) "나는 과천이고 친구들은 성남이랑 강남에 살아서 모이기 편한 곳" → `origin: "과천·성남·강남 중간지점"`, 반경 7.5km, 서초/강남 인근(세 지역의 실제 중간)에서 추천됨 확인 (2) "나는 성남이고 친구는 광주에 살아서..." → 동명이지역 되묻기 발생 → "경기도 광주요" 답변 이어붙여 재요청 → `origin: "성남·경기도 광주 중간지점"`로 정상 확정(위 회귀테스트가 잡은 버그가 실제로 고쳐졌음을 확인) (3) 기존 단일 출발지 문장("지금 과천인데...") → 영향 없이 그대로 동작
- [x] 검증 중 Windows Git Bash의 `curl -d`가 한글 인자를 깨진 인코딩으로 전달해 서버가 실제로는 다른(깨진) 문자열을 받는 문제를 발견 — 애플리케이션 버그 아님, 이후 검증은 Node `fetch` 스크립트로 전환해 재확인
- [x] 사용자가 실사용 중 Gemini 503("high demand", 일시적 과부하) 에러가 원시 JSON 그대로 화면에 노출되는 것을 리포트 — 이번 중간지점 기능과는 무관한 기존 결함(429 할당량 초과만 친절한 문구로 처리돼 있었고 503은 누락돼 있었음). `lib/gemini.js`에 429와 동일한 패턴으로 503 처리 추가("AI 서버가 일시적으로 혼잡합니다..."), `api/recommend.js`가 `err.serviceUnavailable`이면 503으로 응답하도록 수정
