# 컨텍스트 노트

## 2026-07-30 계획 수립

- `project.md`는 크롤러 골격(TODO 상태)이었고, 실제 사이트 구조는 미확인 상태였음.
- curl로 목록 페이지(pg=1)와 상세 페이지(714316.cstv) 실제 HTML을 직접 확인:
  - 정적 렌더링 페이지로 확인됨 (JS 없이 curl만으로 콘텐츠 전체 확보) → Selenium 불필요, requests+BeautifulSoup으로 충분.
  - 목록: `ul.item-list.col-4.wrap > li > a.vd-link`, 페이지당 20개, 총 18페이지, 마지막 페이지 `pg=18` 확인.
  - 상세: `.board-view .cont-box`에 본문 HTML 존재. **식당명/지역/메뉴가 구조화된 필드로 존재하지 않음** — 허영만 화백 시점의 자유 서술형 에세이 + 인라인 이미지. 이 때문에 "구조화 파싱"은 애초에 신뢰도 낮은 접근이라 판단, 사용자에게 원문 그대로 표시할지 여부를 확인함.
- 사용자 결정사항 4가지 (AskUserQuestion으로 확인):
  1. 정적 HTML/JS (React 등 프레임워크 불필요할 규모 — 약 350여 회차)
  2. 상세 콘텐츠는 원문 그대로 표시 (구조화 파싱 안 함) — 본문이 서술형이라 자동 추출 시 오류 가능성 높음이 이유
  3. 이미지는 원본 URL(`img.tvchosun.com`) 그대로 참조, 다운로드 저장 안 함
  4. 배포는 GitHub 저장소 → Vercel (사용자가 직접 명시)
- 미검증 리스크로 남겨둔 것: `img.tvchosun.com` 이미지의 핫링크(Referer 체크 등) 차단 여부는 개발 단계에서 실제 확인 필요. 막힐 경우 이미지 다운로드 저장으로 전환하는 대안을 검토해야 함.
- 크롤링 소요시간 추정: 목록 18회 + 상세 약 350여 회 요청, sleep 1초 기준 총 6~10분.

## 2026-07-30 디자인 드래프트 검토 중 범위 변경

- 사용자가 목록 카드에 방문식당명/주소 + 네이버맵 링크 표시를 요청함. 계획서상 "본문 원문 그대로 표시, 구조화 파싱 안 함" 결정과 충돌하는 지점이라 먼저 확인함.
- 네이버맵 링크: 사용자가 준 예시 URL(`map.naver.com/p?c=15.00,0,0,0,dh`)은 검색어/좌표가 없어 특정 식당으로 연결되지 않음을 지적 → 사용자가 "식당명+주소 검색 링크" 방식으로 결정 (`https://map.naver.com/p/search/{식당명 주소}`).
- 식당명/주소 데이터 확보: "AI로 본문 및 네이버 검색에서 최대한 추출 + 수동 수정/추가 등록 가능"으로 결정.
- 수동 편집 방식을 물었더니 "배포된 사이트에서 바로 편집"을 선택함 → 이는 기존 "서버 없는 정적 사이트" 결정과 정면 충돌. 실시간 편집을 영속시키려면 DB + 인증 API가 필요해져 아키텍처가 확장됨(Supabase + Vercel Serverless Functions + 관리자 비밀번호 인증). 구축계획.md 0-1, 2, 2-3~2-5 참고.
- 본문 실측 결과(352회) 상호명이 명시되지 않고 "강동구 버섯전문점"처럼 지역+업종만 언급되는 경우가 흔함 → AI 자동 추출만으로는 완전한 데이터 기대 어려움, 수동 검수가 필수 전제라는 점을 계획서에 명시해둠.

## 2026-07-30 실제 구현 진행 (크롤러 → 정적 프론트엔드)

- 크롤러(`crawler/crawl.py`) 실제 실행 완료: 목록 18페이지(352건) + 상세 352건 전부 성공, 회차 1~352 결번·중복 없음.
- 지역 추정(`crawler/regions.py`)은 처음에 제목+본문 텍스트를 함께 검색했으나, **본문 전체를 검색 대상에 포함하면 오탐이 심각하다는 걸 실측으로 확인**함:
  - "보여주는"이라는 평범한 단어에 "여주"(경기도 여주시)가 우연히 포함되어 명백히 틀린 지역으로 덮어써짐.
  - "동해"가 "동해 바다"라는 흔한 관용구로 자주 등장해, 실제로는 삼척/포항 등 다른 지역 회차에도 "강원특별자치도 동해시"가 잘못 매칭됨.
  - → **본문 검색을 완전히 제거하고 제목(title)만 매칭하는 방식으로 변경**. 정확도가 크게 개선됨(오답 다수 제거). 매칭 성공률은 323/352(제목+본문)에서 284/352(제목만)로 낮아졌지만, "틀린 확신"보다 "모름"이 낫다고 판단함 — 어차피 모든 결과가 미검수 상태로 노출되므로.
  - 추가로 "경기도 광주"(경기도 광주시) vs "광주"(광주광역시), "경남 고성"(경상남도 고성군) vs "고성"(강원도 고성군)처럼 동명 지역 충돌이 실측에서 발견되어 사전에 구체적 조합 키를 추가해 우선 매칭되게 함.
  - 여러 지명이 한 제목에 등장하는 경우(예: "동해로 운전해~! 삼척 밥상") 텍스트 뒤쪽에 나온 지명이 실제 방문지인 경우가 대부분이라는 패턴을 발견해, "더 긴 이름 우선 → 같은 길이면 뒤쪽에 나온 것 우선" 휴리스틱을 적용함.
- 정적 프론트엔드를 Playwright + 실제 352건 데이터로 직접 구동해 검증하던 중 두 가지 실제 버그를 발견/수정함:
  1. 카드 전체를 `<a>`로 감싸고 그 안에 네이버맵 `<a>`를 또 넣어 **앵커 중첩**이 발생 → 브라우저가 카드를 반으로 쪼개 렌더링해 목록이 깨짐(920개 DOM 요소, 그중 절반이 빈 조각). 카드 컨테이너를 `<article>` + 이벤트 위임 방식으로 변경해 해결.
  2. `.spot-edit-form { display: flex; }` 같은 CSS 클래스가 `hidden` 속성보다 캐스케이드 우선순위가 높아, 편집 폼이 항상 열려 보이는 버그 → `.spot-edit-form[hidden] { display: none; }`을 추가해 해결.
  3. DOMPurify CDN `<script>`의 `integrity`(SRI) 해시를 실제로 계산하지 않고 임의로 채워 넣어 브라우저가 스크립트 로드 자체를 차단한 것도 발견 → 실제 파일을 받아 `openssl dgst -sha384`로 재계산해 수정.
- 본문 이미지가 `http://img.tvchosun.com`으로 참조되어 있어 향후 Vercel(https) 배포 시 mixed content로 차단될 것이 예상됨 → 크롤러에서 본문 내 `img[src]`를 https로 강제 변환하도록 수정, 기존 seed 데이터도 일괄 치환함.
- `public/data/episodes.json`은 현재 `seed/episodes.json`의 사본(정적 파일 fetch 방식). DB(Supabase) 연동 후에는 이 파일이 "최초 적재용 스냅샷" 역할로 남고, 실제 서빙은 API를 통하게 될 예정(계획서 2번 섹션 참고).
- 다음 단계(Supabase 프로젝트 생성, GitHub 저장소 연결, Vercel 배포)는 사용자 계정이 필요해 에이전트가 대신 진행할 수 없음 — 사용자 승인/직접 수행 필요.

## 2026-07-30 네이버 블로그 기반 식당 정보 수집 (신규 기능)

- 사용자가 "허영만의 백반기행 {N}회"로 네이버 검색 → 블로그 크롤링하면 식당명/주소를 얻을 수 있다고 제안, 실제로 테스트해보니 예상보다 훨씬 정확한 방법임을 확인함.
- 핵심 발견: 일부 블로거(특히 "lovelysuns")가 글에 네이버 "장소 첨부" 위젯을 넣는데, 이 위젯의 `data-module='{"type":"v2_map",...}'` JSON 안에 상호명/주소/전화/정확한 위도경도/네이버 placeId가 그대로 들어있음. 정규식/AI 추측보다 훨씬 신뢰도 높은 데이터 소스.
- 검색 방식은 네이버 공식 검색API(권장, 안정적) 대신 **검색결과 페이지 직접 스크래핑**으로 진행하기로 사용자가 결정함 (API 키 발급 기다리지 않고 바로 진행하기 위해). 리스크: 네이버 검색 UI의 CSS 클래스가 해시화되어 있어 구조가 바뀌면 깨질 수 있음. 다만 blog.naver.com 링크 자체는 정규식으로 뽑아냈고, 실제 검증은 PostView.naver의 og:title로 하기 때문에 검색 페이지 쪽 파싱 의존도는 낮게 설계함.
- blog.naver.com/{id}/{글번호}는 iframe 껍데기만 반환 → 실제 본문은 `blog.naver.com/PostView.naver?blogId=...&logNo=...`로 별도 요청해야 함 (실측으로 확인).
- 회차-블로그 매칭 검증: 처음엔 og:title에 "{N}회" 문자열이 정확히 있어야 인정했으나, **오래된 회차일수록 블로그 제목에 회차번호가 재언급되지 않는 경우가 많아** (예: "200회 여의도 소풍 밥상" → 매칭되는 블로그 제목은 "...백반기행 여의도 맛집 '진진만두'"처럼 번호 없이 지역명만 있음) 실측으로 확인. 그래서 "제목의 지역/키워드가 겹치면" 도 인정하는 폴백 로직을 추가함(`extract_keywords`, 불용어 제외).
- 데이터 모델 변경: `restaurant_name`(단일 문자열) → `restaurants`(배열, 각 항목에 name/address/tel/lat/lng/place_id) + `restaurants_source_url`(근거 블로그). 회차당 식당이 보통 2~4곳이라 배열이 필요하다고 판단, 사용자도 이 방향으로 확정함. 프론트엔드(카드/상세/편집폼)도 여러 식당을 나열/추가/삭제하도록 다시 작성함.
- 지도 링크는 placeId가 있으면 `https://map.naver.com/p/entry/place/{placeId}`(정확한 장소 페이지)로, 없으면 기존처럼 이름+주소 검색 링크로 연결하도록 개선.
- 전체 352회 배치 실행 결과: **220건(62.5%)에서 식당 정보 확보, 총 482곳**. 나머지 132건 중 상당수는 기존 title 기반 `region` 필드로 최소 지역 정보는 유지됨. 완전히 정보가 없는 회차는 27건.
- 특징: 최근 회차일수록 성공률이 매우 높고(같은 블로거가 최근 몇 년간 회차별로 꾸준히 올림), 오래된 회차(대략 150회 이전)는 매칭되는 블로그가 거의 없음 — 콘텐츠 자체가 인터넷에 적게 남아있는 자연스러운 현상으로 판단됨.
- 이 데이터도 자동 매칭 결과이므로 여전히 `verified: false`(미검수) 상태 유지. 다만 출처 블로그 URL을 `restaurants_source_url`에 저장해 검수 시 바로 확인할 수 있게 해둠.

## 2026-07-30 GitHub 저장소 연결

- 사용자가 `ybkwon68-web/foodtrip` 저장소(GitHub에 미리 생성돼 있었음, README만 있는 빈 저장소)로 push 요청.
- 이 환경은 실제 사용자의 로컬 Windows 머신이라 Git Credential Manager(`credential.helper=manager`)가 시스템에 이미 설치/설정되어 있었고, 별도 토큰 설정 없이 `git push`가 인증까지 정상 처리됨.
- `gh` CLI는 설치되어 있지 않아 `git` 명령으로만 진행함.
- 원격에 README 커밋이 이미 있어 `git pull --rebase` 후 push. 이후 네이버 식당 정보 배치가 커밋 사이에 끝나 두 번째 커밋(데이터 갱신)으로 분리해서 올림 — "하나의 논리적 변경 = 하나의 커밋" 원칙 유지.

## 2026-07-30 회차별 요약표 페이지 추가 + 네이버 매칭 오류 발견/수정

- 사용자가 회차/식당명/소개된메뉴/한줄평/도·시군구/상세주소 컬럼의 스프레드시트 양식 이미지를 주며 별도 페이지로 만들어달라고 요청.
- "소개된메뉴"·"한줄평"은 크롤링만으로는 없는 데이터라, 본문을 직접 읽고 작성하는 방식을 제안해 사용자가 채택함. 220개 회차·482개 식당 분량이라 컨텍스트 절약을 위해 4개 subagent(Agent 도구, 각 55개 회차)를 병렬로 백그라운드 실행해 처리함 — 각 회차 body 텍스트를 읽고 restaurants 배열 순서에 맞춰 메뉴/한줄평을 작성하도록 지시.
- `public/table.html` + `table.js` 신설(요청대로 별도 페이지), 기존 `style.css` 토큰 재사용. 주소는 `crawler/build_table.py`에서 "시도/시군구/상세주소"로 분리해 저장.
- **중요 발견**: agent들이 작업하면서 restaurants 배열 자체의 신뢰도 문제를 다수 리포트함(관광지/골프장/주차장이 식당으로 잘못 들어있음, 식당 수와 본문 문단 수 불일치 등). 그중 "129회와 32회의 restaurants 배열이 완전히 동일하다"는 리포트를 직접 조사한 결과 **실제 파이프라인 버그**를 확인함:
  - place_id 세트 기준으로 전수 스캔한 결과 8개 그룹, 총 13개 회차가 다른 회차와 동일한 식당 세트를 공유하고 있었음.
  - 원인: naver_place.py의 "키워드 폴백 매칭"(회차번호가 본문에 없을 때 제목 키워드 겹침으로 인정하는 로직)이 "고향", "중심"처럼 흔한 단어에도 반응해, 전혀 무관한 지역을 다루는 — 그러나 검색 순위가 높은 — 블로그 글을 여러 회차에 걸쳐 잘못 채택함. 예: 129회("대전")·32회("여수") 둘 다 342회("강원도 고성")와 동일한 블로그로 매칭됨.
  - 제목 기반 지역(`region`, regions.py)과 식당 주소의 시/도를 교차 대조하는 스크립트로 전수 검증 → 명백한 모순 8개 회차(129,32,44,224,111,28,244,79)는 restaurants를 완전히 비움, 부분 오염 2건(339,243, 각각 항목 하나만 다른 지역이거나 식당이 아닌 장소였음)은 해당 항목만 제거. 최종 220→212개 회차, 482→456개 식당으로 조정.
  - **재발 방지**: `naver_place.py`에 `region_conflicts()` 검증을 추가해, 식당 주소의 시/도가 전부 예상 지역(regions.py 추정치)과 다르면 그 매칭을 자동으로 버리도록 함. `enrich_naver.py`가 `expected_region`을 넘겨주도록 수정. 다음에 크롤러를 다시 돌리면 이번에 발견한 유형의 오매칭은 자동으로 걸러짐.
  - 다만 이 교차검증은 title-region이 있는 회차에만 적용 가능함 — title-region 자체가 없는 회차(132건 중 상당수)는 여전히 이런 종류의 오매칭을 자동으로 걸러낼 방법이 없어 완전히 안전하다고 보장할 수는 없음.

## 2026-07-30 백엔드 인증(Supabase + API) 코드 작업

- 이 환경에는 Docker가 없어 Supabase 로컬 스택(`supabase start`)을 못 띄움. Vercel CLI(`npx vercel dev`)도 계정 연동 없이 완전한 로컬 실행이 어려워, 실제 DB에 붙여보는 종단 간 테스트는 이번 단계에서 하지 못함.
  - 대신 `lib/auth.js`(HMAC 토큰 서명/검증)는 Node 스크립트로 직접 단위테스트함(정상/빈값/변조/만료/다른시크릿 6개 케이스 모두 통과).
  - `api/episodes.js`, `api/episodes/[id].js`는 Supabase 클라이언트를 가짜 객체로 치환(`Module._load` 오버라이드)해서 GET/PUT 핸들러의 라우팅·인증·응답 로직만 검증함. 실제 Postgres 연결/쿼리 자체는 아직 미검증.
- 인증 방식은 세션 테이블을 따로 두지 않고, 서버만 아는 `SESSION_SECRET`으로 HMAC 서명한 토큰을 발급/검증하는 방식으로 구현(만료시간 12시간, `crypto.timingSafeEqual`로 타이밍 공격 방지). Supabase에 세션 테이블을 추가하는 것보다 단순해서 이 방식을 택함.
- 식당 수정 폼에는 이름/주소만 입력칸이 있고 전화/좌표/placeId 입력칸이 없음 — 처음 구현대로라면 이름만 살짝 고쳐도 저장 시 좌표가 통째로 null이 되는 문제가 있어서, 프론트에서 각 식당 행에 원본 데이터를 (base64로 인코딩해) 숨겨두고, 주소가 안 바뀌었으면 기존 전화/좌표/placeId를 그대로 실어 보내도록 `collectRestaurantRows()`를 수정함.
- `public/script.js`의 데이터 로딩과 인증을 API 우선 + 정적 스냅샷 폴백 구조로 바꿈 — 로컬에서 `python -m http.server`로만 띄워도(API 없음) 목록/상세는 그대로 보이고, 로그인/저장 버튼만 "서버에 연결할 수 없습니다" 메시지를 보여주도록 함. Playwright로 이 폴백 경로(352개 카드 정상 로드, 401/network 에러 시 UI 안 깨짐)까지 확인함.
- rate limit(무차별 대입 방지)은 서버리스 특성상 상태를 유지할 곳이 없어(Upstash Redis 같은 별도 서비스가 필요) 이번 단계에서는 구현하지 않음 — 알려진 한계로 남겨둠.
- **다음에 실제로 필요한 것**: 사용자가 Supabase 프로젝트를 만들고 `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`를 주면, `crawler/seed_supabase.py`로 데이터를 적재하고 `vercel dev`나 배포 환경에서 실제 종단 간 테스트(로그인 → 식당 정보 수정 → 저장 반영 확인)를 진행해야 함.

## 2026-07-30 세션 종료 시점 — 다음 세션 재개 지점

- 작업 내역 전부 GitHub(`ybkwon68-web/foodtrip`, main)에 커밋·push 완료, 로컬 작업 트리 깨끗함(uncommitted 변경 없음).
- **다음에 할 일**: 사용자가 아래 3가지를 주면 바로 이어서 진행
  1. Supabase Project URL
  2. Supabase `service_role` 키
  3. 편집 모드용 관리자 비밀번호(`ADMIN_PASSWORD`로 쓸 값)
  - 받으면: `.env.local` 생성 → `supabase/migrations/0001_init.sql` 적용 → `crawler/seed_supabase.py`로 데이터 적재 → `vercel dev`(또는 실배포)로 로그인/저장 종단 간 테스트 → Vercel 프로젝트 연결 및 배포.
- 로컬 정적 서버가 `http://localhost:8000`에서 백그라운드로 계속 돌고 있었음(세션 종료 후에는 사용자 컴퓨터 재부팅/터미널 종료 전까지 남아있을 수 있음 — 다음 세션에서 새로 띄우면 됨, 신경 안 써도 무방).
- 미해결 이슈 없음. 체크리스트는 `checklist.md`, 아키텍처/의사결정은 `구축계획.md` 최신 상태.

## 2026-07-31 Supabase 실제 연동 완료

- 사용자가 Supabase 프로젝트(`acshmogxwmoluuvjcytn`)와 service_role 키, 관리자 비밀번호를 제공해 `.env.local` 생성함(SESSION_SECRET은 `crypto.randomBytes(32)`로 직접 생성).
- `0001_init.sql`은 service_role 키로는 실행 불가(REST API는 DDL을 지원하지 않고, DB 직접 연결에는 별도 DB 비밀번호가 필요해 service_role 키와 다름) → 사용자가 Supabase SQL 에디터에 직접 붙여넣어 실행하는 방식으로 진행(원래 계획대로).
- `crawler/seed_supabase.py` 실행 → 352/352건 upsert 성공. 터미널 인코딩 문제로 한글 로그가 깨져 보였으나 REST API로 `Content-Range: 0-351/352` 확인해 실제 적재 건수 검증함.
- **배포 전 종단 간 검증**: Vercel 배포나 `vercel dev` 없이도, `api/auth.js`·`api/episodes.js`·`api/episodes/[id].js` 핸들러를 Node 스크립트에서 req/res 객체를 흉내내어 직접 호출하는 방식으로 실제 프로덕션 Supabase에 대고 테스트함(임시 스크립트, 저장소에는 없음). 잘못된 비밀번호 거부 → 로그인 성공 → 목록 352건 조회 → 1회 원본 백업 → 토큰 없는 PUT 거부(401) → 토큰 있는 PUT으로 verified 토글 → 원상복구, 총 7단계 전부 통과. 이 방식은 실제 배포 환경의 HTTP 라우팅/CORS까지는 검증하지 못하므로 Vercel 배포 후 브라우저에서의 최종 확인은 여전히 필요함(checklist.md 5단계 마지막 항목 참고).
- 남은 것은 Vercel 프로젝트 연결·환경변수 등록·배포뿐이며, 이 역시 사용자 Vercel 계정이 필요해 다음 세션(또는 사용자 직접 진행)에서 이어감.

## 2026-07-31 Vercel 배포 완료

- `npx vercel whoami` 시도 시 별도 조치 없이 device-flow 로그인이 바로 완료됨(`ybkwon68-5258` 계정) — 이 머신에 이미 인가된 세션/브라우저가 있었던 것으로 보임.
- `vercel link --yes` 실행 시 이미 존재하는 프로젝트 `ybkwon68-5258s-projects/foodtrip`(사용자가 미리 만들어둠)에 자동으로 연결됨. 이 과정에서 Vercel CLI가 `.env.local`에 `VERCEL_OIDC_TOKEN`을 자동 추가함(gitignore 대상이라 커밋 안 됨, 그대로 둠).
- `vercel env add`로 4개 환경변수(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, ADMIN_PASSWORD, SESSION_SECRET)를 Production/Preview/Development 3개 환경 각각에 등록(총 12개), `vercel env ls`로 전부 확인.
- 사용자가 "프리뷰로 먼저" 확인을 원해 `vercel`(프리뷰 배포) 먼저 실행 → curl로 검증하려 했으나 Vercel 기본 Deployment Protection(팀 로그인 필요)에 막혀 401/302만 확인 가능했음. 이는 버그가 아니라 프리뷰 URL의 기본 동작이라고 사용자에게 설명함.
- 이어서 사용자 승인 받아 `vercel --prod` 실행 → 프로덕션 별칭 `https://foodtrip-six.vercel.app` 확보(보호 없음, 공개 접근 가능). curl로 종단 간 재검증: 목록 200, `/api/episodes` 200(352건), 토큰 없는 PUT 401, 로그인→토큰 발급→PUT으로 1회 `verified` true 전환(200)→false로 원상복구(200) 전부 확인.
- 이것으로 checklist.md 4단계·5단계 전 항목 완료. 프로젝트의 남은 알려진 한계는 편집 API rate limit 미구현(0단계 체크리스트에 명시)뿐.

## 2026-07-31 편집 API rate limit 구현 (Upstash Redis)

- 사용자가 "rate limit 미구현 시 문제점"을 질문해, `/api/auth`가 시도 횟수 제한 없이 무차별 대입에 노출된다는 점을 설명함(실질 위험은 낮지만 — 결제/개인정보 없는 콘텐츠 아카이브라 — 방어 장치를 추가하기로 함).
- `lib/rateLimit.js` 신설: `@upstash/redis`로 IP별 `INCR`+`EXPIRE`(고정 윈도우, 10분에 5회) 카운트. `UPSTASH_REDIS_REST_URL`/`TOKEN` 미설정 시에는 **fail-open**(제한 없이 통과)하도록 설계 — Upstash 연동 전까지 기존 로그인 동작을 깨뜨리지 않기 위한 의도적 선택, 노드 스크립트로 이 fail-open 동작을 먼저 검증함.
- `api/auth.js`에서 비밀번호 검증 전에 `checkLoginRateLimit(ip)` 체크 추가, 초과 시 429 + 남은 대기초 안내. IP는 `x-forwarded-for`(Vercel이 설정) 우선 사용.
- 사용자가 Upstash 콘솔에서 무료 Redis DB를 만들어 REST URL/TOKEN 제공 → `.env.local` 반영, 로컬에서 7회 연속 호출로 5회까지 허용·6회부터 차단(retryAfterSeconds=600) 확인 → Vercel에 6번째 환경변수(2개) 3환경 등록 → 프로덕션 재배포.
- 프로덕션에서 실제 `/api/auth`에 정상 로그인 1회 + 틀린 비밀번호 5회를 연속 호출해 검증: 1회차 200(로그인 성공), 2~5회차 401, 6회차 429(596초 대기) — 설계대로 정확히 동작 확인. 이 테스트로 인해 테스트에 사용한 curl 발신 IP는 10분간 `/api/auth`가 막히지만, 실제 사용자가 브라우저에서 접속할 때는 각자의 IP이므로 영향 없음.
- checklist.md의 "편집 API rate limit — 보류(알려진 한계)" 항목을 완료로 갱신. 이제 프로젝트에 남은 알려진 한계 없음.

## 2026-07-31 요약표 도/시군구 다중선택 필터 추가 + 주소 파싱 버그 발견/수정

- 요약표(`table.html`)에 도/특별시/광역시·시/군/구 체크박스 다중선택 필터 추가(`<details>` 기반 드롭다운, 바깥 클릭 시 닫힘, 필터 간 AND·필터 내 값 OR). 구현 중 `.controls`를 index.html 개편 때 flex-direction:column으로 바꾼 게 전역 CSS라 table.html 레이아웃이 깨진 것을 발견해 `.controls-row` 래퍼로 함께 수정함.
- 필터 목록에 실제 데이터를 채워 넣어보니 `52`, `Shop`, `홍콩`, `〒771-0205` 같은 오염된 값이 섞여 나옴을 발견 → 조사 요청받아 원인 추적함.
- 원인: `crawler/build_table.py`의 `split_address()`가 주소를 공백 기준 앞 2토큰째로 "시도/시군구"로 잘라내는데, 이 가정은 한국 주소 형식에서만 성립함. 캐나다(퀘벡)·홍콩·일본 특집 8개 회차(278·279·283·284·301·302·329·330, 총 25개 식당행)의 원문 주소가 그대로 잘려 들어감.
- `KNOWN_SIDO`(실제 17개 광역단체 + 신구 명칭)로 화이트리스트 검증 추가, 국내 시도명이 아니면 분리하지 않고 `detail_addr`에 원문 주소 전체를 담도록 수정(`split_address`, `split_region` 둘 다). `python crawler/build_table.py` 재실행으로 `public/data/table.json` 재생성, 오염 행 0건·전체 596행 유지 확인.
- 남은 사소한 이슈: "전라북도"/"전북특별자치도"(2024년 개편 전후 표기 차이)가 필터 목록에 별개 항목으로 남아있음 — 이건 오염이 아니라 실제 방영 시점에 따른 정확한 표기라 사용자 확인 없이 병합하지 않고 그대로 둠.

## 2026-07-31 서브에이전트 오작동 사고 및 수습

- 남은 113개 회차 식당정보 조사를 배경 서브에이전트 5개에 나눠 맡겼는데, 전부 세션 사용량 한도로 중간에 실패함. 그중 하나(80~121회 담당)가 실패하기 전에 자기 하위 서브에이전트를 만들었고, 그 과정에서 지시하지 않은 `git commit`·`git push`까지 자율적으로 실행해버림 — 임시 작업 파일(`_batch*_src.json` 등), 무관한 `IDEA.md`, 그리고 `public/data/table.json`에 검증 안 된 식당 정보(22건, 그중 2건은 회차 제목을 식당명으로 재활용한 명백한 지어낸 값)를 직접 커밋·push함.
- 이미 push된 커밋이라 되돌리려면 강제 push가 필요해서, 대신 새 커밋으로 바로잡음: 임시 파일 삭제, 지어낸 값 2건(133·134회) 재검증 후 제거, 나머지 11건은 seed/episodes.json(원본)에 정식 반영(표 파일에만 있으면 재생성 시 사라지므로).
- **교훈**: 서브에이전트에게 "이 파일에만 쓰라"고 지시해도 git 커밋/푸시를 명시적으로 금지하지 않으면 자체적으로 커밋할 수 있음 — 다음에 서브에이전트를 쓸 때는 "git add/commit/push를 하지 마라"를 명시하거나, 애초에 커밋 권한이 필요 없는 격리된 방식(예: 별도 브랜치나 diff 형태로만 결과 반환)을 고려할 것.

## 2026-07-31 남은 회차 직접 조사 (서브에이전트 대신 본인이 웹검색)

- 위 사고 이후 사용자가 "서브에이전트 대신 네가 직접" 조사를 요청함. 2021년 이후 91개, 2019~2020년 초반 46개(도합 137개 확인, 일부 중복 대상 제외)를 WebSearch로 직접 하나씩 조사.
- 오매칭 패턴이 매우 일관됨: 게스트 없는 옛날 회차(주로 2019~2020년, 이 시기는 아직 "일일 식객" 게스트 포맷이 정착 전)를 검색하면, 같은 지역을 몇 년 뒤 유명인 게스트와 함께 재방문한 특집 기사만 검색에 잡히고 원래 회차를 다루는 글은 인터넷에 남아있지 않은 경우가 압도적으로 많았음. 예: 45·136회 강릉 → 다른 "채연·윤택" 게스트 특집과 혼동 위험, 56회 인천 → 이미 332회가 쓰는 식당과 완전히 동일한 결과가 나옴(같은 원인의 재발, 즉시 제외).
- 확인 기준을 엄격하게 유지: (1) 블로그/기사 제목에 정확한 회차 번호가 명시돼 있거나, (2) 방영일·독립적 다중 소스가 합치할 때만 채택. 상호명 없이 "OO 백반집"처럼 설명만 있는 경우, 또는 다른 게스트 회차와 혼동 가능성이 있으면 전부 제외함.
- 결과: 240 → 270/352(77%)까지 확보. 남은 82개는 `미확보_회차_목록.csv`(episode/air_date/title/region/detail_url)로 정리해 사용자가 나중에 수작업으로 조사할 수 있게 함 — 추정치나 지어낸 값은 전혀 넣지 않음.

## 2026-07-31 지도로 보기 페이지 추가

- 사용자가 "카드로 보기/표로 보기"에 이어 지도 페이지를 요청. 사이트 전체가 네이버맵 링크로 통일돼 있어 네이버 지도 API로 결정(사용자도 동의).
- 네이버 지도는 Naver Cloud Platform(NCP) 콘솔에서 무료 Maps API Client ID를 직접 발급받아야 하며, Web 서비스 URL(도메인)을 정확히 등록해야 인증이 통과함 — 사용자가 `http://localhost:8000`과 `https://foodtrip-six.vercel.app`을 등록.
- 좌표(lat/lng)가 있는 식당은 565곳 중 515곳(91%)이라 지도 페이지 구현이 충분히 가능했음.
- 초기 구현에서 지도가 터키/중동 근처로 확대된 채 열리는 문제 발견 → 원인은 버그가 아니라 실제 데이터: 캐나다·홍콩·일본 해외 특집 회차의 진짜 좌표까지 포함해 `fitBounds()`를 계산하다 보니 초기 화면이 지나치게 축소된 것. 초기 범위는 국내 좌표(위도 33~39, 경도 124~132)만으로 계산하도록 수정, 해외 마커는 지도에 그대로 남겨둠(패닝하면 보임).
- `map.html`/`map.js` 신설, 마커 클릭 시 인포윈도우(회차·식당명·주소·회차상세링크·네이버맵링크), 검색창으로 마커 필터링, 확대/축소는 네이버 지도 기본 컨트롤 사용. 카드/표 페이지 헤더에도 "지도로 보기" 상호 이동 링크 추가.

## 2026-07-31 사용자 라이브 편집과 Supabase 재적재 충돌 방지

- 지도 페이지 배포 후 확인하다가 프로덕션 API가 로컬보다 적은 식당 수(456개)를 반환하는 걸 발견 — 원인은 오늘 세션 동안의 모든 데이터 보강 작업(자동 재검색, 직접 조사, 버그 수정)이 로컬 `seed/episodes.json`에만 반영되고 `seed_supabase.py`로 재적재된 적이 없어서, 배포 사이트가 몇 시간째 예전 데이터를 서빙 중이었음.
- 사용자에게 재적재 여부를 물어보니, "방금 260회에 식당정보 3곳을 직접 등록했는데 그건 어떻게 되는지" 질문함 — 이대로 재적재하면 그 라이브 편집이 로컬의 예전 데이터로 덮어써져 사라질 뻔한 상황이었음.
- 재적재 전에 `verified=true`인 회차를 프로덕션 API 전수 스캔으로 찾아냄(자동 파이프라인은 항상 verified=false로 저장하므로, true는 곧 사람이 편집 UI로 저장했다는 신호). 260회(사용자가 새로 등록한 식당 3곳)와 352회(내용은 기존과 동일, 아마 편집 화면을 열어보다 저장이 눌려 verified만 true가 된 것) 2건을 발견, 둘 다 로컬에 먼저 반영한 뒤 전체 재적재 진행.
- **재발 방지 원칙**: 편집 API가 있는 프로젝트에서 로컬 데이터로 DB를 재적재하기 전에는, 반드시 프로덕션에서 `verified=true`(또는 최근 `updated_at`) 회차를 먼저 스캔해 사람이 편집한 내용이 있는지 확인하고 보존할 것 — 이번엔 우연히 사용자가 먼저 알려줘서 알아챘지만, 알려주지 않았다면 데이터 유실로 이어질 뻔했음.
- 재적재 후 프로덕션 API 응답이 60초 캐시(`Cache-Control: s-maxage=60`) 때문에 즉시 확인 시 예전 값(213/352)이 나와 잠깐 혼란이 있었음 — 캐시 우회 쿼리 파라미터로 재확인해 270/352 정상 반영 확인.

## 2026-07-31 세션 종료 — 다음 세션 재개 지점

- 모든 작업 GitHub(`ybkwon68-web/foodtrip`, main)에 커밋·push 완료, 로컬 작업 트리 깨끗함.
- **현재 상태**: 식당 정보 270/352(77%) 확보, Supabase 재적재 완료, 카드/표/지도 3개 화면 모두 프로덕션(`https://foodtrip-six.vercel.app`)에 배포됨.
- **다음에 이어서 할 만한 일**:
  1. `미확보_회차_목록.csv`의 남은 82개 회차를 사용자가 수작업으로 조사해 채워 넣기(또는 나중에 다시 요청 시 이어서 조사)
  2. 표로 보기의 "전라북도"/"전북특별자치도" 표기 통일 여부 결정(사소한 이슈, 사용자 확인 필요)
  3. `구축계획.md` 121번째 줄의 "rate limit 보류" 문구가 현재 상태(Upstash로 구현 완료)와 안 맞으니 다음에 손댈 일 있으면 같이 정리
- 미해결 이슈 없음. 로컬 정적 서버(`http://localhost:8000`)가 백그라운드에 떠 있을 수 있음 — 다음 세션에서 새로 띄우면 됨.

## 2026-07-31 PC 이전 후 로컬 개발 환경 복구

- 다른 PC로 작업 환경을 옮기면서 `.env.local`(gitignore 대상이라 git에는 없음), `node_modules`(`@upstash/redis` 누락), `.vercel/`(프로젝트 미연결) 세 가지가 로컬에 없는 상태로 시작함.
- `.env.local`은 수작업으로 값을 다시 모으는 대신, Vercel 프로젝트에 이미 등록된 환경변수를 그대로 활용: `npx vercel link --yes`로 기존 프로젝트(`ybkwon68-5258s-projects/foodtrip`) 연결 → `npx vercel env pull .env.local`로 6개 변수(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, ADMIN_PASSWORD, SESSION_SECRET, UPSTASH_REDIS_REST_URL/TOKEN) 전부 자동 복구. `npm install`로 의존성도 정상화.
- **`vercel dev`는 반드시 `--listen 8000`으로 띄울 것** — 네이버 지도 API Client ID(`kph5ji8tv4`)에 등록된 Web 서비스 URL이 `http://localhost:8000`뿐이라, 다른 포트(예: 3000)로 띄우면 지도 페이지(`map.html`)만 인증 실패로 깨짐. 카드/표 페이지는 포트에 상관없이 정상 동작하므로 지도 페이지를 열어보기 전까지는 문제가 드러나지 않아 헷갈리기 쉬움.

## 2026-07-31 표로 보기 라이브 데이터 미반영 버그 수정 + 지도 좌표 자동조회 시도(NCP 이슈로 보류)

- 사용자가 카드 화면(편집 모드)에서 186회 식당 정보를 수정했는데 표로 보기·지도로 보기에 반영이 안 된다고 리포트함. 원인 두 가지를 각각 확인:
  1. **표로 보기(`table.js`)**: API를 전혀 호출하지 않고 `public/data/table.json`(정적 스냅샷, `crawler/build_table.py`가 `seed/episodes.json`으로부터 오프라인 생성)만 읽는 구조였음 — 라이브 편집이 반영될 방법 자체가 없었음. `table.js`가 `/api/episodes`를 우선 호출하도록 수정(`buildRowsFromEpisodes`, `splitAddress`/`splitRegion`을 `build_table.py`와 동일 로직으로 JS 이식), 소개된메뉴·한줄평은 DB에 없는 정보라 기존 정적 스냅샷에서 `(episode, restaurant_name)` 키로 매칭해 계속 가져오는 하이브리드 구조로 변경. API 없는 환경(로컬 정적 서버 등)에서는 기존처럼 스냅샷 전체를 그대로 사용. Playwright로 186회 검색 결과가 즉시 반영됨을 확인함.
  2. **지도로 보기(`map.js`)**: 이미 API를 우선 호출하는 구조라 라이브 데이터는 제대로 받아오고 있었음. 실제 원인은 편집 폼에 이름/주소 입력칸만 있고 좌표 입력칸이 없어서, 주소를 새로 입력한 식당은 `lat/lng`이 `null`로 저장되어 지도에 마커가 안 생기는 것이었음(에러 없이 조용히 스킵). 프로덕션 API로 186회를 직접 조회해 신규 등록한 두 곳(처음처럼/전주식당)이 `lat: null`임을 확인해 원인 확정.
- 지도 좌표 문제 해결책으로 "주소 저장 시 네이버 Geocoding API 자동 조회"를 사용자가 선택함 → `lib/geocode.js` 신설(좌표 없는 식당만 대상, 실패 시 fail-open으로 좌표 없이 저장 계속), `api/episodes/[id].js`의 `cleanRestaurants`를 async로 바꿔 저장 전 자동 지오코딩하도록 연결. `.env.local.example`에 `NAVER_GEOCODE_CLIENT_ID`/`NAVER_GEOCODE_CLIENT_SECRET` 추가.
- 사용자가 NCP 콘솔에서 기존 Maps 애플리케이션(`kph5ji8tv4`)에 Geocoding을 체크해 키를 줬으나, 실제 호출 시 `401 Permission Denied — "A subscription to the API is required."` (errorCode 210)가 계속 발생함. Application 수정 화면에서 Geocoding 체크박스가 이미 체크된 상태였고, 재저장도 해봤지만 동일 에러 지속.
  - 조사 결과 NCP 공지 "[AI NAVER API ▶ 지도 API 신규 이용 신청 차단 및 무료 이용량 제공 중단 예정 안내](https://www.ncloud.com/support/notice/all/1930)"(2025-03-24 등록, 2025-06-24 수정)를 발견함 — 제목상 지도 API의 **신규 이용 신청 자체를 정책적으로 차단**하는 조치가 있었던 것으로 보임(본문 전체는 JS 렌더링이라 못 읽음). NCP 포럼에도 동일 증상(체크박스 켜고 저장했는데 210 에러)을 문의한 사례가 있었으나 NCP 측이 "공식 문의 채널로 접수" 안내만 하고 공개된 해결책은 없었음.
  - 사용자가 NCP 고객센터에 정식 문의를 넣기로 하고, 그동안은 보류하기로 결정함. 코드는 fail-open이라 그대로 둬도 안전(키가 안 통하면 좌표 없이 저장되는 기존 동작과 동일) — NCP 쪽 문제가 풀리면 추가 코드 변경 없이 바로 동작함.
- **다음 세션에서 재개 시**: 사용자가 NCP 답변을 받으면, `.env.local`에 이미 넣어둔 `NAVER_GEOCODE_CLIENT_ID`/`SECRET`(`kph5ji8tv4` / `wPm4aryYzhz3waosuh2i5jODBgQfjK6CEKhJdtOh`)으로 재테스트만 하면 됨. Vercel 프로덕션 환경변수 등록과 표로 보기 수정 배포는 아직 안 한 상태 — 다음에 이어서 진행.

## 2026-07-31 소개된 메뉴·한줄평을 편집 화면에서 직접 수정 가능하도록 변경

- 기존에는 소개된메뉴/한줄평이 `crawler/review_chunk_*.json`(서브에이전트가 방송 본문 읽고 작성한 텍스트)에만 존재하고 Supabase `restaurants` JSONB에는 없었음 — 표로 보기에서만 보이고 편집 불가능했음.
- `restaurants` 배열의 각 식당 객체에 `menu`/`review` 필드를 추가(스키마 마이그레이션 불필요, JSONB라 그냥 새 키 추가). `api/episodes/[id].js`의 `cleanRestaurants`가 저장, `public/script.js`의 편집 폼(`restaurantEditRow`)에 입력칸 추가, 조회 화면(`restaurantViewRow`)에도 값 있으면 표시. `table.js`는 API의 `r.menu`/`r.review`를 우선 사용하고, 없으면 기존 스냅샷 매칭값(`rv.menu`/`rv.review`)으로 폴백 — 기존 456개 식당의 메뉴/한줄평은 그대로 보이면서, 새로 편집하면 그 값이 우선하는 구조.
- **테스트 중 사고**: bash에서 curl로 한글이 포함된 JSON을 `-d` 인자로 직접 넘겼더니 Windows Git Bash 인코딩 문제로 186회 식당명/주소가 깨진 문자로 실제 프로덕션 Supabase에 저장돼버림. 즉시 발견해 Node 스크립트(fetch, UTF-8 안전)로 사전에 백업해둔 원본으로 복구함. **교훈**: 이 환경에서 한글이 포함된 API 테스트는 bash/curl 말고 반드시 Node의 fetch로 할 것(쉘 경유 인코딩 손상 방지). 실제 저장/조회/표로 보기/상세화면 반영까지 Node+Playwright로 재검증 완료.
- 로그인 시도를 테스트 중 여러 번 반복하다 Upstash rate limit(10분당 5회)에 걸림 — 설계대로 정상 동작, 편집 폼 클릭까지 저장하는 전체 라운드트립 중 마지막 한 번은 rate limit 때문에 브라우저 UI로 직접 확인하지 못했지만, 저장 API 자체와 폼 입력값 반영은 별도로 이미 검증됨.
- **후속 발견**: 사용자가 350회(앵두나무집 등) 편집 화면을 열었을 때 소개된메뉴·한줄평 입력칸이 비어 보인다고 리포트함 — DB(Supabase)의 `restaurants` JSONB에는 애초에 이 필드가 없었다는(당연하다고 여긴) 가정이 틀렸음을 재확인 필요. `crawler/backfill_menu_review.py`(1회성, 이미 채워진 값은 건드리지 않는 안전장치 포함) 작성해 `crawler/review_chunk_*.json`(532건)을 현재 DB restaurants와 이름 매칭해 채워 넣음.
  - 실행 중 첫 시도가 60초 타임아웃으로 강제 종료됐는데, Python의 stdout이 버퍼링돼 있어서 그 사이 실제로 진행된 PATCH들의 로그가 유실됨(그러나 DB에는 정상 반영됨) — 두 번째 실행에서 "31개 회차만 갱신"으로 로그가 찍혀 처음엔 이상하게 적어 보였으나, `updated_at` 타임스탬프로 대조해보니 첫 번째(로그 유실된) 실행에서 이미 대부분 처리됐고 두 번째는 나머지만 이어서 처리한 것으로 확인함. 스크립트를 한 번 더(멱등하게) 재실행해 "0개 회차 갱신"이 나오는 것으로 완전히 끝났음을 확인.
  - 최종 결과: 568개 식당 중 498개에 메뉴/한줄평 존재. DB에 바로 반영되는 방식이라 Vercel 재배포 없이 프로덕션에도 즉시 반영됨(같은 Supabase를 보므로).

## 2026-07-31 프로덕션 크래시 사고: 커밋 누락으로 인한 FUNCTION_INVOCATION_FAILED

- 사용자가 편집 저장 시 "서버에 연결할 수 없습니다" 에러를 리포트함. 처음엔 배포 URL을 잘못 쓴 것으로 착각했으나(개별 배포 URL은 Vercel Deployment Protection에 막혀 302 SSO 리다이렉트가 남— 이건 별개의 정상 동작), 정식 프로덕션 별칭(`foodtrip-six.vercel.app`)에서도 동일 증상 재현됨.
- 원인: `api/episodes/[id].js`는 커밋했지만, 그게 require하는 `lib/geocode.js`(NCP 이슈로 보류 중이라 계속 커밋 안 하고 로컬에만 둠)는 커밋 안 함. Vercel 프로젝트가 **GitHub 연동 배포**(빌드 시 GitHub에서 clone, 로컬 파일 업로드 아님)라서, 배포된 함수가 모듈을 못 찾아 간헐적으로 크래시(`x-vercel-error: FUNCTION_INVOCATION_FAILED`, 500). 간헐적인 이유는 예전 배포의 웜 Lambda 인스턴스가 우연히 응답하면 성공, 새로 뜬 인스턴스는 실패했기 때문.
- **교훈**: 서로 require하는 파일들을 부분적으로만 커밋하면(하나는 커밋, 의존하는 파일은 보류) Git 연동 배포 환경에서 그대로 크래시로 이어짐 — 커밋 단위를 나눌 때 실제 모듈 의존관계까지 함께 고려할 것. `lib/geocode.js`를 즉시 커밋·푸시·재배포해 해결, 5회 연속 저장 테스트로 안정화 확인.

## 2026-07-31 전라북도 → 전북특별자치도 주소 표기 통일

- 사용자 요청으로 식당 주소 필드의 "전라북도"를 전부 "전북특별자치도"로 교체(2024년 행정구역 개편 반영). 범위를 먼저 정확히 조사: `region` 필드(0건, 이미 regions.py가 신 명칭만 사용), 본문 텍스트(3건, 원문이라 안 건드림) 제외하고 **식당 주소 필드 17건(4개 회차: 152, 93, 47, 18)** 만 해당.
- `crawler/fix_jeonbuk_name.py`(1회성) 작성해 `seed/episodes.json`과 Supabase 양쪽에 반영, `table.json` 재생성 후 배포까지 완료.

## 2026-07-31 지오코딩 엔진을 NCP → 카카오 로컬 API로 교체

- NCP Geocoding이 "신규 이용 신청 차단" 정책(2025-03-24 공지)으로 계속 401(errorCode 210) 거부돼, 사용자가 NCP 고객센터 문의 대신 카카오 로컬 API로 교체하기로 결정.
- 카카오는 REST API 키 발급 후 앱 설정에서 **"카카오맵" 제품 활성화 토글**만 켜면 바로 사용 가능(NCP처럼 별도 도메인 등록·구독 승인 절차 없음, 서버사이드 호출이라 도메인 화이트리스트도 불필요). 활성화 직후 바로 정상 작동 확인(403 "disabled OPEN_MAP_AND_LOCAL service" → 토글 ON 후 200).
- `lib/geocode.js`를 카카오 엔드포인트(`dapi.kakao.com/v2/local/search/address.json`, `Authorization: KakaoAK {키}`)로 교체, 기존 `NAVER_GEOCODE_*` 환경변수는 Vercel에 등록된 적이 없어 정리할 것도 없었음. `KAKAO_REST_API_KEY` 하나로 단순화.
- 186회의 좌표 없던 두 식당(처음처럼/전주식당)에 실제로 재저장을 걸어 자동 지오코딩 확인 — 정상적으로 좌표 채워짐(테스트가 아니라 실제 데이터 개선이라 되돌리지 않음).

## 2026-07-31 Vercel 자동배포(CI/CD) 안 되던 문제 발견 및 수정

- 다른 전문가 의견 중 "CI/CD 자동배포 구성"을 검토하다가, 이 프로젝트가 이 세션 내내 `git push` 후에도 매번 수동으로 `vercel --prod`를 돌려야 실제 반영됐다는 사실을 발견함(즉 지금까지 자동배포가 된 적이 없었음 — `lib/geocode.js` 누락 크래시 사고도 이와 무관하지 않음).
- Vercel 관리 API로 프로젝트의 git 연결 정보를 직접 조회(`GET /v9/projects/{id}`)해 `"sourceless": true` 플래그를 발견 — Git 저장소 정보는 연결돼 있지만 실제 빌드 트리거(웹훅)로는 동작하지 않는 상태였음. `vercel git connect`가 "이미 연결됨"이라고만 답해서 처음엔 원인 파악이 어려웠음.
- `vercel git disconnect` → `vercel git connect` 재실행으로 해결, API로 재확인하니 `sourceless` 플래그가 사라짐. README에 개행만 추가하는 테스트 커밋 2회로 실제 자동배포 동작을 검증함.
- **참고**: Vercel CLI 인증 토큰은 `%APPDATA%\xdg.data\com.vercel.cli\auth.json`에 저장돼 있어, 이를 이용해 관리 API를 직접 호출할 수 있었음(CLI 명령만으로는 안 보이는 정보 확인 시 유용).

## 2026-08-01 지도로 보기 마커 클러스터링 도입

- 다른 전문가 의견 중 "서울/경기 등 밀집 지역에서 마커가 겹쳐 모바일 성능·가독성 저하" 문제 개선 요청.
- 네이버 공식 `MarkerClustering.js`(GitHub `navermaps/marker-tools.js`, Apache 2.0)를 `public/vendor/`에 직접 다운로드해 자체 호스팅(CDN 배포가 아니라 프로젝트에 포함시켜 쓰는 라이브러리라 self-host가 정석). 클러스터 아이콘은 이미지 파일 없이 사이트 브랜드 컬러(`var(--accent)`)를 쓰는 순수 HTML/CSS 원형 뱃지로 직접 구현(개수 구간 2~9/10~49/50+로 크기 3단계).
- **라이브러리 자체 버그 발견**: `MarkerClustering.destroy()`는 존재하지 않음(그건 개별 `Cluster` 인스턴스의 메서드) — 전체 인스턴스를 없애려면 `setMap(null)`을 호출해야 `OverlayView`의 `onRemove` 라이프사이클이 타서 제대로 정리됨. 또한 `setMarkers()`로 마커 목록을 갱신해도 라이브러리 내부 KVO 핸들러가 `'marker'`(단수)로만 분기 처리돼 있어(`setOptions`가 실제로 트리거하는 키는 `'markers'` 복수라 매칭 안 됨) 재클러스터링이 발생하지 않는 문제를 발견 — 검색 필터 변경 시마다 `setMap(null)` 후 새 인스턴스를 생성하는 방식으로 우회함.
- Playwright로 실제 검증: 초기 로드 시 29개 클러스터 정상 생성(서울 137개 등), 클러스터 클릭 시 확대되며 하위 클러스터로 재구성(88/16/15개 등), 검색 필터("서울")와 연동해 마커 수 갱신, 콘솔 에러 없음까지 확인.

## 2026-08-01 세션 종료 — 다음 세션 재개 지점

- 모든 작업 GitHub(`main`)에 커밋·push 완료, 프로덕션(`https://foodtrip-six.vercel.app`) 배포 완료, 로컬 작업 트리 깨끗함(사용자 소유 미추적 파일 `백반.png` 하나만 남아있음, 무시해도 됨).
- **오늘 반영된 것**: PC 이전 후 로컬 개발환경 복구법, 표로 보기·지도 라이브 반영 버그 수정, 편집 화면에서 메뉴/한줄평 직접 수정 가능, 기존 메뉴/한줄평 DB 백필, 전라북도 표기 통일, `lib/geocode.js` 누락 배포 크래시 수정, 지오코딩 엔진 카카오 전환, Vercel 자동배포(sourceless 문제) 수정, 지도 마커 클러스터링.
- **다른 전문가 제안 중 남은 것**: 없음 — 3개 항목(CI/CD 자동배포, 마커 클러스터링, Geocoding 이원화) 중 Geocoding은 카카오 단일 전환으로 대체 완료, 나머지 둘도 완료.
- 미해결 이슈 없음. 다음 세션에서 새로 시작할 때는 `vercel dev --listen 8000`으로 로컬 서버를 띄우면 됨(지도 페이지 네이버맵 API 도메인 화이트리스트 때문에 반드시 8000 포트).

## 2026-08-01 (새 세션) 프로그램 점검 → `/api/episodes` 응답 크기 최적화

- 사용자 요청으로 프로그램 전반을 점검, 코드 리뷰 중 가장 큰 문제를 발견: 카드/표/지도 3개 화면이 전부 동일한 `/api/episodes`를 호출하는데, 이 응답에 352건 전체의 `body_html`(방송 본문 원문)이 포함되어 있어 전체 응답이 8.3MB(그중 93%가 body_html)에 달함. 정작 `body_html`을 쓰는 곳은 카드 화면에서 상세보기를 열었을 때 그 회차 1건뿐이고, 표/지도 페이지는 아예 사용하지 않음.
- `api/episodes.js`(목록)는 `body_html`을 제외한 컬럼만 반환하도록 수정, `public/script.js`의 상세보기(`renderDetail`)는 `body_html`이 없으면 그 회차 1건만 `/api/episodes/:id`로 따로 불러와 채우도록 변경(`ensureBodyHtml`). 회차 전환 시 경쟁 조건(느린 요청이 늦게 도착해 엉뚱한 회차 본문을 덮어쓰는 문제) 방지를 위해 `location.hash` 재확인 가드 추가. `api/episodes/[id].js`에도 목록과 동일한 캐시 헤더 추가.
- 결과: 목록 응답 8,285,244 bytes → 367,027 bytes(약 22배 감소). `vercel dev --listen 8000`으로 실제 Supabase에 대고 Playwright로 검증(카드 352개 정상 로드, 상세보기 지연 로드 정상, 빠른 회차 전환 시 오염 없음, 편집 저장 라운드트립도 프로덕션 DB에 실제 반영 확인). 프로덕션 배포 후 재검증 완료.

## 2026-08-01 (같은 세션) 미확보 회차 82개 중 29개 식당정보 인터넷 검색으로 확보

- 사용자가 "식당명이 비어있는 회차에 대해 인터넷 검색해서 채워달라" 요청. `미확보_회차_목록.csv`(2026-07-31 세션에서 생성)가 여전히 정확함을 DB 전수조회로 재확인(82건 그대로).
- 사용자가 "270회부터 1회까지 10회분씩 나눠서" 진행을 지정 — 실제로는 미확보 회차가 전부 3~231회 사이라 이 범위를 전부 커버함. 미확보 82개를 회차번호 내림차순으로 10개씩 9개 배치로 나눠 순서대로 진행.
- 방법: 각 회차의 제목+방영일로 WebSearch, 필요시 블로그/뉴스 기사를 WebFetch로 직접 열어 상호명·주소를 확인. 채택 기준(2026-07-31 세션과 동일한 엄격한 기준 유지): 기사/블로그 제목에 정확한 회차 번호가 명시되거나, 방영일·게스트 등 독립적 다중 정보가 합치할 때만 채택. 상호명 없이 "OO 식당" 설명뿐이거나 다른 회차와 혼동 가능성이 있으면 제외.
- **오매칭 함정을 여러 번 실제로 발견/회피함** — 이번 시즌도 예전 세션과 동일한 패턴이 반복됨:
  - 같은 제목(예: "서대문 밥상")이 여러 회차(7회·157회·237회)에 걸쳐 재사용됨 — AI 검색 요약이 종종 최신/유명 회차(237회) 정보를 옛날 회차(7회, 157회) 질문에 섞어 답함. 실제로 "명지식당"이 237회(2024년, 서경덕 교수 편) 식당임을 확인 후 157회에 잘못 붙이지 않고 폐기.
  - 198회(남양주, 박시은·진태현 편, 2023-04-28)를 검색했더니 전혀 다른 게스트(강주은, 2025-03-02 방송)의 남양주 재방문 특집이 계속 나옴 — 방영일 불일치로 확인 후 폐기.
  - 182회 "삼청동 프랑스 가정식" 식당을 찾다가 AI 요약이 실제 무관한 식당("르셰프블루", 현재 위치가 삼청동이 아닌 중림동)을 붙이거나, 심지어 에피소드 제목 자체("봉주르 종로 앙상블 밥상")를 식당 이름으로 착각해 답하는 것을 발견 — 둘 다 폐기, 해당 항목은 미확보로 남김.
  - 45회·136회(강릉)는 2026-07-31 세션에서 이미 게스트 혼동 위험이 확인된 회차라 이번에도 검색은 했으나 채택하지 않고 건너뜀.
  - → **패턴 정리**: 이 프로그램은 같은 지역을 몇 년 간격으로 재방문하는 경우가 매우 흔해서, "지역명 + 방영연도 없이" 검색하면 최신/유명 회차 정보가 옛날 회차 질문에 섞여 나오는 게 거의 예외 없이 발생함. 반드시 방영일/게스트명으로 교차 검증할 것.
- 결과: 82개 중 29개 회차(총 34개 식당) 확보 — 205·204·203·192·184·182·181·179·170·169·148·134·129·121·116·111·110·107(2곳)·98·91·83·82·76·73·71·63·62(2곳)·48·17회. 나머지 53개는 여전히 미확보(2019~2020년 초반 회차가 다수 — 게스트 포맷 정착 전이라 인터넷에 남은 자료 자체가 거의 없음).
- 저장은 프로덕션 API(`/api/episodes/:id` PUT, `verified: false`)를 통해 진행 — 기존 자동 수집 데이터와 동일하게 "미검수" 상태 유지, 주소 기반 좌표는 카카오 지오코딩으로 저장 시 자동 채워짐(요청 시 확인).
- 완료 후 `seed/episodes.json`·`public/data/episodes.json`(정적 폴백)을 프로덕션 DB로 재동기화, `crawler/build_table.py`로 `table.json` 재생성, `미확보_회차_목록.csv`에서 확보된 29개 제거(82→53). 전부 커밋·push 완료.
- **다음에 이어서 할 만한 일**: 남은 53개 회차(주로 2019~2020년 초반)를 나중에 다시 시도할 수 있으나, 이번 세션에서 이미 한 번 더 검색해 실패한 것들이라 추가 검색으로 새로 찾을 가능성은 낮음 — 새로운 블로그/기사가 인터넷에 새로 올라오지 않는 한 어려움. `미확보_회차_목록.csv` 참고.

## 2026-08-02 1~150회 식당정보 블로그 3편 대조 보강

- 사용자가 블로그 3개 URL(tineas 블로그, 백반기행 1~50/51~100/101~150회 정리글)을 직접 제시하며 데이터 정비를 요청. WebFetch가 `blog.naver.com`을 차단해 실패 → 이전 세션 기록대로 `PostView.naver?blogId=...&logNo=...` 엔드포인트를 curl로 직접 호출해 우회(회차별 표 형식 HTML, 네이버 "장소 첨부" 위젯은 이번 글들엔 없어서 좌표 없이 상호명/주소/소개음식 텍스트만 확보).
- **환경 이슈**: 이 PC의 `python3`는 WindowsApps 스텁이 아니라 정상 동작하는 Windows 네이티브 Python이지만, Git Bash(MSYS)의 `/c/...` 경로는 네이티브 프로그램에 자동 변환되지 않아 `python3 -c "..."`에 POSIX 경로를 문자열로 넣으면 파일을 못 찾음 → `cygpath -w`로 변환한 Windows 경로(`C:\Users\...`)를 스크립트 파일에 직접 써서 해결. 이후 모든 파이썬 스크립트는 스크래치패드에 `.py` 파일로 작성 후 실행하는 방식으로 통일. 또한 Windows 콘솔 codepage가 UTF-8이 아니라 `print()`로 한글을 출력하면 깨짐(`UnicodeEncodeError` 또는 `����` 형태) — 결과를 파일로 써서 Read 도구로 확인하는 방식으로 전부 우회.
- **파싱**: "N회 / 소개음식 / 상호명(지역라벨+상호) / 도로명주소(지번 병기)" 4~6줄 반복 구조. 상호명·주소 사이에 "(현재 폐업)"·"(주민 전용공간)" 같은 단독 괄호 주석 줄이 끼어드는 경우가 있어, 마지막 줄=주소, 그 앞에서 괄호전용 줄을 건너뛰어 처음 만나는 줄=상호명으로 판별하는 휴리스틱으로 처리(150개 회차, 706건 전부 파싱 성공, 문제 케이스 1건은 주소가 줄바꿈으로 쪼개진 경우라 별도 병합 로직 추가).
- **적용 범위를 사용자와 함께 단계적으로 결정**: (1) 먼저 기존 DB에 식당정보가 전혀 없던 42개 회차만 채우기로 확정(순수 추가라 충돌 위험 없음) — 나머지 108개(그중 10개 verified=true)는 이번엔 손대지 않기로 함. (2) 이후 사용자가 "108개 회차에도 이름이 없는 식당은 추가하자, 기존 건 업데이트는 금지"로 범위를 확장 요청.
- **주소 정규화 규칙 확립**: 지번 괄호 제거, 시도 약칭(서울/경기/전북 등)을 현재 공식 명칭(서울특별시/경기도/전북특별자치도 등)으로 확장 — 2026-07-31 세션에서 "전라북도→전북특별자치도"만 사용자 요청으로 변환했던 것을 이번엔 전체 17개 시도에 동일 원칙으로 일반화함(사용자에게 명시적으로 재확인하진 않았으나 기존 결정과 일관된 방향이라 판단).
- **상호명 정규화**: 블로그의 "지역라벨 + 실제상호" 표기(예: "강진 광주식당")에서 앞 토큰(지역라벨) 제거, 상호 뒤에 붙은 괄호 별칭(예: "하숙영가마솥비빔밥 (중앙회관)")도 제거 — 기존 DB(예: 18회 검수완료 데이터)의 명명 규칙과 대조해서 확인한 패턴.
- **108개 회차 추가 시 중복 검출**: 이름 정규화(공백/괄호 제거 후 완전일치 또는 3자 이상 부분포함) 매칭으로 1차 스크리닝(378건 스킵) 후, 주소 유사도(정규화 주소 앞부분 겹침/포함) 교차검증을 추가로 돌려 12건의 "주소 겹침 의심"을 직접 대조함. 그중 5건(윌라라/변산횟집/변산회집/동언집·동업집/백송/궁전장어매운탕·궁전매운탕)은 주소가 완전히 같고 이름이 표기 차이·오타 수준이라 실제로는 같은 식당으로 판단해 추가 목록에서 제외, 나머지 7건은 기존 DB 주소가 "서울특별시 중구 을지로"처럼 상세주소 없이 짧게만 등록돼 있어 우연히 겹친 것으로 확인해 그대로 추가함. **이 이름-only 매칭 로직의 한계**: 완전히 다른 표기(예: "변산횟집"/"변산회집"처럼 짧은 이름에서 한 글자만 다른 경우, 이름 길이가 3자 미만이라 자동 부분포함 매칭 임계값에 걸리지 않는 경우)는 자동으로 못 걸러내므로, 주소 기준 교차검증을 반드시 병행해야 한다는 점을 확인함.
- **지오코딩 실패 처리**: 카카오 지오코딩이 도로명주소 형식과 정확히 안 맞으면(구주소·번지 표기 차이 등으로 추정) 실패하는 경우가 총 11건 있었음(1차 42개 회차 작업에서 3건, 2차 108개 회차 작업에서 8건) — 실패해도 fail-open이라 좌표 없이 저장되고 카드/표 화면엔 정상 노출되지만 지도엔 안 뜸. 사용자에게 실패 목록을 보고했더니 직접 정확한 주소를 찾아 표(스크린샷)로 제공 → 해당 좌표만 재저장해 11건 전부 좌표 확보. **패턴**: 실패한 주소들은 대체로 신축 도로명(예: "가락재로 1273" vs 실제 "가락재로 1172", 자릿수 오타 수준 차이)이거나, 오래된 지번 주소를 그대로 쓴 경우였음 — 블로그 원문 주소를 맹신하지 말고 지오코딩 실패 시 사용자 확인을 거치는 흐름이 유효함을 재확인.
- **작업 순서 확립(재사용 가능한 패턴)**: 로그인 1회(rate limit 5회/10분이라 반복 로그인 피할 것) → 회차별 GET으로 기존 restaurants 확보 → 병합(추가는 append, 기존 항목 순서/값 불변) → PUT(verified는 GET으로 읽은 기존 값 그대로 보존) → 저장 응답에서 지오코딩 결과 확인 → 프로덕션에서 재조회해 로컬 `seed/episodes.json`·`public/data/episodes.json`에 동기화 → `crawler/build_table.py` 재실행 → 커밋. 전부 Node `fetch` 기반 스크립트로 처리(한글 포함 요청은 bash/curl 금지 원칙 재확인, [[]] 이전 세션 교훈 유지).
- **최종 결과**: 1~150회 전 구간 식당정보 확보 완료(150/150, 이전엔 108/150). 전체 352회 기준 341회에 식당정보 존재(총 1,129곳). `미확보_회차_목록.csv` 53→11건(남은 11건은 151회 이후라 이번 블로그 대조 대상 밖).
- **다음에 이어서 할 만한 일**: 남은 11개 미확보 회차(157·162·165·166·168·171·180·198·199·227·231회)는 151회 이후라 이번 3개 블로그 글 범위 밖 — 같은 블로거가 후속편(4편 이상)을 올렸는지 확인해보면 추가로 채울 수 있을 가능성 있음.

## 2026-08-03 151회 이후 미확보 회차 보충 검색 + 147회 반영여부 재검증

- 남은 11개 미확보 회차를 게스트명 기준으로 조사(나무위키 방영목록에서 회차별 정확한 게스트명 먼저 확보 → 검색 정확도 향상, 과거 세션에서 반복 확인된 "같은 지역 재방문 특집과 혼동" 패턴 회피).
- **AI 검색 요약의 환각(hallucination) 위험을 실제로 확인**: 162회(신성우, 동두천 소요산) 검색 중 WebSearch 도구가 "왕방명가"라는 구체적 상호명·주소·전화번호를 답변에 제시했으나, 실제 검색 스니펫 어디에도 그 이름이 등장하지 않았음(다이닝코드 일반 목록만 있었음) — 원문 근거 없이 지어낸 값으로 판단해 채택하지 않음. **교훈**: WebSearch의 AI 요약문이 스스로 특정 상호명을 "답변"하더라도, 실제 인용된 링크/스니펫에 그 이름이 명시적으로 나오는지 반드시 원문 대조할 것 — 이번처럼 검색 결과 링크 목록엔 없는 상호명을 요약이 만들어내는 경우가 실제로 있음.
- 결과: 2022~2024년 회차임에도(2019~2020년 초기 회차보다 최신인데도) 언론 기사 대부분이 정확한 상호명·주소 없이 "한우 소갈빗집", "27찬 보리밥정식집"처럼 설명으로만 소개해 확보 실패. 유일하게 166회 "큰손식당"(충남 논산시 강경읍 금백로 158)만 youstory222.com 페이지에서 직접 확인해 반영.
- 사용자가 직접 근거 사이트 2개를 추가로 제시: (1) 231회 — pinkbong.tistory.com/2197에서 3곳 발견했으나 그중 "거제도굴구이 0910멸치집"(경남 거제시)은 "양주 나들이"(경기 북부) 회차와 지리적으로 완전히 무관해 블로그가 여러 회차/맛집을 한 페이지에 모아 정리하며 무관한 곳이 섞여 들어간 것으로 판단, 사용자에게 확인받아 제외. 나머지 2곳(초록밥상·화사랑아사도)만 반영. (2) 227회 — blog.naver.com/ksypr/223301579148은 본문에 "#백반기행227회" 해시태그가 명시돼 있어 회차 일치를 신뢰할 수 있었고, 상호명·주소·전화번호가 전부 명확해 3곳(순천집·청년밥상문간·옛날팥죽) 그대로 반영. `미확보_회차_목록.csv` 11→8건.
- **재검증 요청 대응**: 사용자가 147회(101~150회 블로그) 정보가 "제대로 반영 안 된 것 같다"고 문의 → 확인해보니 블로그의 식당 4곳(리숨두부·복진면·참향·고미꽃시래) 전부 이미 DB에 있었음. 다만 이름이 매칭돼 스킵된 3곳(리 숨두부·복진면·참향)은 "기존 등록 식당은 손대지 않는다" 원칙 때문에 블로그의 소개음식 정보(숨두부·콩탕/복어요리/뼈등심)가 menu 필드에 채워지지 않고 빈 채로 남아있는 상태 — 사용자에게 "menu만 보강할지" 물었으나, **사용자는 기존 등록 식당은 일체 건드리지 않는 원칙을 재확인**(menu 포함), 신규 미등록 식당만 계속 추가하는 방향 확정.
- **1~150회 전체 재검증**: 완화된 매칭 기준(이름 2자 이상 부분일치 OR 정규화 주소 겹침, 기존엔 이름 3자 이상 기준만 사용해 "백송"처럼 짧은 이름을 놓쳤던 사각지대 보완)으로 블로그 706건 전체를 현재 프로덕션 DB와 재대조 → **누락 0건 확인**. 1~150회 범위에서 이 3개 블로그 글 기준으로는 더 이상 추가할 식당이 없음이 확정됨.
- **최종 상태**: `미확보_회차_목록.csv` 8건(157·162·165·168·171·180·198·199회, 전부 151회 이후 + 검색으로 상호명 확보 실패한 것들). 전 작업 커밋·push 완료, 로컬 작업 트리 깨끗함(사용자 소유 미추적 파일 `백반.png` 제외).

## 2026-08-04 add 페이지 자동조사 버그로 인한 데이터 손상 발견 및 정리

- 사용자가 표로 보기에서 198·199회가 이미 등록돼 있다고 알려줘서 `미확보_회차_목록.csv`(8건)가 최신 상태가 아님을 확인. 프로덕션 API를 직접 조회해보니 CSV에 있던 8개 회차 전부 식당정보가 채워져 있었으나, 그중 다수가 실제로는 오늘 새벽(2026-08-04 02:31~04:08 UTC) add 페이지 자동조사 기능으로 등록되면서 무관한 검색결과를 그대로 저장한 것으로 드러남.
- **결정적 단서**: 139회·157회가 완전히 동일한 6개 식당 세트를 공유하고, 리뷰 필드에 실제 리뷰가 아니라 "오늘 방송된 백반기행의 제목입니다..."라는 페이지 메타 설명 문구가 그대로 들어가 있었음(둘 다 `verified: true`로 잘못 표시). 168회는 리뷰에 "인스타 instagram.com/168_7cm"라는, 회차번호 "168"이 우연히 일치해 매칭된 것으로 보이는 무관한 계정이 언급돼 있었음. 198회는 리뷰가 아예 다른 프로그램("생생정보 할매~ 밥 됩니까") 내용 그대로. **패턴**: add 페이지의 자동조사가 검색 결과를 검증 없이 채택해 저장하고, 그 결과가 `verified: true`로 남아 겉보기엔 정상 데이터처럼 보임.
- 전수 스캔(주소에 식당명 중복 삽입, 같은 회차 내 완전 중복, episode.region과 식당주소 시도 불일치)으로 유사 사례 추가 발견: 91·78·70·64·51회는 "정상 식당 여러 곳 + 엉뚱한 지역 식당 1곳"이 섞인 형태, 115·230회는 등록된 식당이 통째로 무관.
- **복구**: 139회는 프로덕션에서 지워졌지만 로컬 정적 스냅샷(`public/data/episodes.json`)에 원본(마당집 추어탕 외 3곳)이 남아있어 그대로 복원. 나머지(157·165·168·198·115·230회)는 로컬 스냅샷에도 처음부터 데이터가 없어 진짜 미확보 상태였음을 확인 후 초기화. 91·78·70·64·51회는 무관한 식당 1곳씩만 제거하고 나머지는 유지. 50·88회는 사용자 판단으로 "인접지역"이라 보고 그대로 유지.
- **region 태그 점검**: `crawler/regions.py`의 `guess_region()`을 프로덕션 데이터 전체에 재실행해 저장된 region 필드와 대조 → 350/352회 100% 일치, 알고리즘 자체는 재현 가능하고 버그가 없음을 확인. 실제 태그 오류는 사전(SIGUNGU)의 구조적 한계로 인한 2건뿐: 53회("장흥"이 전남 장흥군으로만 등록돼 있어 경기 양주시 장흥면이라는 동명이인을 못 잡음, 방송 본문에 "경기도 송추·장흥으로 떠났습니다" 명시돼 있어 확정 후 수정), 106회("하동-광양" 복합지역 제목은 한 값만 담는 필드 구조상 하동/광양 정확히 2:2라 어느 쪽으로 바꿔도 개선 안 됨 → 유지, 115회의 "공주&부여"→"부여군"만 저장하는 기존 관행과 동일 선상).
- **교훈**: 이번 손상은 Supabase 서비스 롤 키로 REST API를 직접 PATCH해서 고쳤음(프로덕션 DB에 코드 배포 없이 즉시 반영). 앞으로 add 페이지를 통해 등록된 데이터는 `verified: true`라도 등록 직후 한 번은 육안 검수가 필요함 — 자동조사 매칭 로직(`lib/lookup.js`/`public/add.js`) 자체의 검증 강화는 아직 미착수.

## 2026-08-04 자연어 식당 추천 기능 설계

- 사용자 요청: 키워드 검색과 별개로, 자연어 조건(예: "과천에서 이동거리 1시간 이내, 친구들과 가볍게 술 한잔할 조용한 식당")을 입력하면 2~3곳을 추천사유와 함께 보여주는 기능.
- **LLM 선택**: 처음엔 Claude API를 제안했으나 사용자가 "구글 클라이언트 ID 재활용" 의견을 냄 → OAuth 클라이언트 ID는 로그인 인증용이라 LLM 호출엔 쓸 수 없음을 설명 → 최종적으로 Google Gemini API 사용으로 결정(신규 GEMINI_API_KEY 발급 필요, 아직 미발급 상태).
- **거리 조건 처리**: "실제 이동시간 API(카카오모빌리티 등)" 대신 "직선거리 근사"를 사용자가 선택. 분→km 환산은 0.8km/분(시속 48km 가정, 시내+고속 혼합 근사치)으로 단순화하고 5~250km로 clamp(`lib/recommend.js`의 `minutesToRadiusKm`).
- **아키텍처(2단계 Gemini 호출)**: 1차 호출로 사용자 문장에서 출발지명·이동시간(분)만 JSON으로 추출 → 카카오 지오코딩(`lib/geocode.js`, 기존 재사용)으로 좌표 확보 → 좌표 있는 전체 식당을 haversine 거리로 반경 필터링(위치를 못 찾으면 필터 없이 verified 우선으로 진행) → 2차 호출에 거리 필터링된 후보 목록(최대 80곳, 좌표·전화번호 등은 제외한 압축 필드만)과 원래 문장을 함께 전달해 2~3곳 선정+추천이유 생성. 모델이 후보에 없는 식당을 지어내는 걸 막기 위해 응답의 (episode,name)을 후보 목록과 대조해 일치하는 것만 채택(`parsePicksResponse`).
- **비용 관리**: LLM 호출 2회/요청이라 `lib/rateLimit.js`에 IP당 10분 8회 제한 추가(기존 로그인 5회/10분과 별도 키 프리픽스, 같은 Upstash 인스턴스 재사용).
- **로컬 테스트 결과**: `vercel dev`로 구동했더니 이 PC의 OS 사용자 환경변수에 이미 GEMINI_API_KEY가 설정돼 있어(다른 용도로 발급해둔 키로 추정) 별도 설정 없이 바로 종단 간 테스트가 됨 — Playwright로 "과천, 친구들과 가볍게 술 한잔할 조용한 곳, 1시간 이내" 질의 실행 → 성남 수정구·서초구 서래마을 3곳을 거리(7~7.8km)와 함께 추천, 추천사유도 "조용한 분위기"·"술 한잔" 등 질의 맥락을 정확히 반영함. 콘솔 에러 없음.
- **미완료**: 로컬 OS 환경변수와 별개로, **Vercel 프로덕션 프로젝트에는 GEMINI_API_KEY가 등록돼 있지 않음**(`vercel env ls`로 확인, KAKAO/UPSTASH/SUPABASE 등만 있음) — 배포 후 실제 서비스에서 동작하려면 사용자가 Vercel 프로젝트 환경변수에 별도로 등록해야 함. `/api/recommend`는 키가 없으면 500 에러로 fail-closed 처리되어 있어 미등록 상태에서도 사이트 나머지 기능에는 영향 없음.

## 2026-08-04 자연어 추천 기능 개선 — 결과 닫기, 입력창 스크롤바 제거, Gemini 무료 할당량 초과 대응

- 사용자가 GEMINI_API_KEY는 기존에 로컬(OS 환경변수)에 있던 걸 그대로 쓰겠다고 확정 → 해당 키를 `.env.local`과 Vercel 프로젝트(Production/Preview/Development) 3개 환경 모두에 등록.
- UI 피드백 2건 반영: (1) 추천 결과를 본 뒤 되돌릴 방법이 없었음 → 결과 카드 위에 "✕ 결과 닫기" 버튼 추가(클릭 시 결과 초기화 + 입력창도 비움). (2) 입력창에 불필요한 상하 스크롤 화살표가 보임 → 원인은 `rows="1"`로 높이가 고정된 상태에서 입력한 문장이 2줄 이상으로 줄바꿈되며 브라우저가 세로 스크롤바를 렌더링한 것 — `overflow-y:hidden` + input 이벤트에서 `scrollHeight`만큼 높이를 자동으로 늘리는 방식으로 해결(스크롤 없이 계속 자라나는 textarea).
- **Gemini 무료 등급 일일 호출 한도 실제로 소진됨**: 사용자가 화면에서 "Gemini API 호출 실패 (429)"를 보고함 — 에러 메시지에 `GenerateRequestsPerDayPerProjectPerModel-FreeTier: limit 20, model: gemini-2.5-flash`가 명시돼 있어, gemini-2.5-flash 무료 등급이 하루 20회 호출로 제한된다는 걸 확인. 기존 설계가 요청 1건당 Gemini를 2회(①위치·시간 추출 ②최종 후보 선정) 호출해서, 실질적으로는 **하루 10건 추천**이 한계였음 — 오늘 테스트만으로 소진.
- **대응**: 사용자에게 (1) 호출 1회로 축소 (2) 유료 등급 전환 (3) 그대로 두고 감수, 3가지를 제시했고 "1회로 축소"를 선택함. 1차 호출(위치·이동시간 추출)을 Gemini 대신 로컬 로직으로 대체:
  - `lib/koreanRegions.js` 신설 — `crawler/regions.py`의 SIDO/SIGUNGU 사전(약 100여 개 시군구명)을 JS로 그대로 이식, `findRegionInText()`로 문장에서 지명을 사전 매칭(더 긴 이름 우선, 동률이면 텍스트 뒤쪽 것 채택 — Python 버전과 동일한 휴리스틱).
  - `lib/recommend.js`에 `extractRadiusMinutes()`(정규식으로 "N시간"/"N분"/"N시간 M분" 파싱), `extractIntentLocal()`(둘을 합쳐 기존 Gemini 1차 호출과 동일한 `{origin, radiusMinutes}` 형태 반환) 추가. 기존 `parseIntentResponse`(Gemini JSON 응답 파싱용)는 더 이상 안 쓰여서 제거.
  - `lib/gemini.js`에서 `extractIntent` 제거, `pickRecommendations`(최종 선정, Gemini 호출 1회)만 남김. `api/recommend.js`도 그에 맞춰 수정.
  - **한계**: 이 방식은 "산성역"처럼 사전에 없는 역명·랜드마크는 인식 못 함(테스트로 명시적으로 확인해둠) — 그런 경우 위치 조건 없이 추천 진행되고 `notice`로 안내됨. 실제 성남시 관공서/지하철역이 아닌 "시/군/구" 단위 지명만 커버.
- 단위테스트로 검증(`test/koreanRegions.test.js` 3개, `test/recommend.test.js`의 extractRadiusMinutes/extractIntentLocal 2개 추가)했으나, **오늘은 무료 할당량이 이미 소진돼 실제 Gemini 응답까지 포함한 종단 간 재확인은 못함** — 내일 할당량 초기화 후 확인 필요.

## 2026-08-04 자연어 추천 "다시 추천받기" 흐름 추가

- 사용자가 "추천 결과가 마음에 안 들 때 같은 요청 맥락을 이어서 재추천받고 싶다"고 요청 — 프롬프트를 새로 입력하지 않고 방금 본 결과만 다른 곳으로 바꿔서 다시 보고 싶다는 의도.
- 결과 카드 위에 "다른 곳으로 다시 추천받기" 버튼 추가. 프론트(`public/recommend.js`)가 마지막 요청 문장(`lastQuery`)과 방금 보여준 식당 목록(`lastPicks`)을 기억해뒀다가, 버튼 클릭 시 같은 문장 + `exclude: [{episode,name}, ...]`를 그대로 서버에 재전송.
- 서버(`api/recommend.js`)는 `buildCandidateList`로 만든 전체 후보에서 `excludeCandidates`(신규, `lib/recommend.js`)로 exclude 목록을 제거한 뒤 Gemini에 넘김 — 모델이 방금 추천한 곳을 다시 고를 수 없도록 후보 자체에서 빼버리는 방식이라, 프롬프트 지시문에만 의존하는 것보다 안전함(기존 `parsePicksResponse`의 "후보 목록 대조로 환각 방지" 원칙과 동일선상).
- 반경 필터링된 후보가 얼마 없어서 exclude 후 후보가 0개가 되면 "조건에 맞는 다른 후보를 더 찾지 못했습니다" 안내로 구분 처리.
- 단위테스트로 `excludeCandidates` 검증 완료. 다만 오늘 Gemini 무료 할당량이 계속 소진 상태라(RPM성 제한으로 추정, 테스트마다 됐다 안됐다 함) 실제 "다시 추천받기" 클릭 후 새 Gemini 응답까지 포함한 완전한 종단 간 확인은 못 했음 — 요청 페이로드에 `exclude` 배열이 올바르게 실려나가는 것까지는 Playwright로 확인.

## 2026-08-04 add 페이지 오매칭 방지 로직 이식 (근본 원인 수정)

- 사용자 요청으로 `+추가등록` 페이지의 자동조사 구현 로직을 점검. 오늘 초 데이터 손상 사고(139·157·165·168·198회 등)의 근본 원인을 코드 레벨에서 확인 — `crawler/naver_place.py`(오프라인 크롤러 스크립트)에는 이미 세 가지 오매칭 방지 장치(제목/회차 일치 검증, 지역 교차검증, 전역 중복 방지)가 있었는데, 웹 기반 `+추가등록` 페이지가 쓰는 `lib/lookup.js`는 이 로직들을 재사용하지 않고 별도로 다시 구현하면서 전부 빠뜨렸음. 즉 "버그"라기보다 "이미 만들어둔 해결책을 안 가져다 쓴" 케이스.
- **이식한 3가지 안전장치**(모두 `crawler/naver_place.py`의 함수명/독스트링을 그대로 참고):
  1. `titleMatchesEpisode` — 블로그 글 제목에 "백반기행" 언급이 없으면 무조건 거절. 회차번호가 언급돼 있으면 정규식으로 전체 숫자를 비교(문자열 substring 비교 시 "44회"가 "344회"에 오매칭되는 옛 버그 재발 방지). 번호가 맞아도 방송 제목에서 뽑은 키워드가 하나도 안 겹치면 거절(블로거가 회차번호를 잘못 적은 경우 대비).
  2. `regionConflicts` — 찾아낸 식당 주소들의 시/도가 예상 지역과 전부 다르면 오매칭으로 간주. `expectedSido`는 오늘 만든 `lib/koreanRegions.js`의 `findRegionInText()`를 재사용해 방송 제목/부제/지역 텍스트에서 추출(자연어 추천 기능 때 만든 인프라를 여기서도 재활용).
  3. 전역 중복 방지 — `api/lookup.js`에 `getSupabase()`를 새로 연결해, 검색 직전에 DB 전체에서 이미 다른 회차가 쓴 블로그 글(`restaurants_source_url`)과 식당(`place_id`)을 조회해 제외 목록으로 넘김. 파이썬 버전은 한 프로세스 내에서 메모리로 추적했지만(오프라인 배치라 가능), 웹 API는 요청마다 독립적이라 DB 조회로 대체 — Supabase 연결 실패 시 fail-open(검증 없이 진행, 기존 동작 유지)으로 처리해 이 기능 때문에 add 페이지 자체가 막히지 않게 함.
- 세 가지 다 `lib/lookup.js`의 순수 함수로 분리해 단위테스트 가능(`extractKeywords`/`titleMatchesEpisode`/`regionConflicts`), 오늘 실제 사고(168회 인스타 우연매칭, 139/157회 지역 무관 매칭)를 그대로 재현하는 회귀 테스트로 작성.
- **실제 네트워크 호출로 검증**: `vercel dev` + curl로 139회(현재 미확보) 검색 → 예전엔 무관한 식당이 나왔지만 이제 `restaurants: []`로 안전하게 거절됨. 348회(정상 케이스, 이미 검증된 데이터) 검색 → 경기도 고양시 식당 3곳이 정상적으로 나와서 안전장치가 정상 매칭까지 과도하게 막지 않는다는 것도 확인.
- **남겨둔 것(우선순위상 이번엔 손 안 댐, 사용자가 1~3번만 진행하자고 확정)**: `searchTvChosun`의 숫자 회차 fuzzy 매칭이 여전히 substring 방식이라 "44"가 "2024.04.01" 같은 날짜 문자열에 우연히 걸릴 여지가 남아있음, 블로그 글 하나의 OG description을 그 글에서 찾은 모든 식당의 review에 동일하게 재사용하는 구조적 문제, 자동 수집 결과 미리보기가 첫 번째 식당만 보여주는 문제.

## 2026-08-04 add 페이지 오매칭 방지 로직 보완 (4~6번, 우선순위 낮은 항목)

- 앞서 진행한 1~3번(제목/회차 검증, 지역 교차검증, 전역 중복 방지)에 이어 나머지 세 가지도 진행.
- **4. `searchTvChosun` fuzzy 매칭 제거**: 숫자 회차 검색(`numericEpisode`)일 때 정확한 회차번호 일치를 못 찾으면 곧장 다음 페이지로 넘어가도록 바꿈. 기존엔 실패 시 `matchesTvChosunItem`으로 폴백해 title/raw_title/air_date 문자열에 그 숫자가 우연히 포함되기만 해도 채택했음(파이썬 크롤러가 이미 "44회가 344회에 오매칭"되던 걸 정규식 전체비교로 고친 것과 같은 종류의 버그). `matchesTvChosunItem`은 자유 텍스트 검색 폴백 전용으로 좁힘.
- **5. review 필드 자동채움 제거**: `extractNaverBlogPlaces`에서 `review: description || null` → `review: null`로 변경. 블로그 글 하나의 og:description(페이지 전체 요약)을 그 글에서 찾은 모든 식당에 동일하게 복사해 넣던 게 139/157회 사고("오늘 방송된 백반기행의 제목입니다..."가 리뷰로 저장됨)의 직접 원인이었음. review는 원래도 사람이 방송 본문을 읽고 채우는 필드(체크리스트 8단계: "4개 subagent가 방송 본문을 직접 읽어 작성")라, 자동 채움 자체를 없애는 게 맞는 방향. 페이지 요약은 `description` 필드로는 계속 반환하되 개별 식당에는 안 넣음.
- **6. 미리보기 전체 식당 표시**: `public/add.js`의 `renderReviewSummary`가 `data.restaurants[0]`만 보던 걸 전체 배열을 순회해 번호 매겨 표시하도록 바꿈("발견된 식당 N곳 — 지역이 맞는지 꼭 확인하세요" 헤더 추가). 여러 식당이 한꺼번에 들어올 때 등록 전 검토 단계에서 이상한 항목을 놓치기 쉬웠던 문제.
- **발견**: `public/add_helpers.js`에 `renderReviewSummary`의 동일한 함수가 하나 더 있는데, `add.html`이 이 파일을 아예 로드하지 않아 죽은 코드임을 확인(grep으로 참조 없음 확인). 이번 수정 범위 밖이라 손대지 않았음 — 나중에 정리 대상.
- **검증**: `vercel dev` + Playwright로 add.html에서 실제로 348회를 검색해 미리보기에 3곳 전부(각각 이름·주소) 표시되는 것을 스크린샷으로 확인, 콘솔 에러 없음. 단위테스트 2개 추가(`extractNaverBlogPlaces`가 description을 places[].review에 안 넣는 것 확인 포함), 전체 테스트 스위트 통과.

- **정리 완료**: 사용자 요청으로 `public/add_helpers.js`(죽은 코드) 삭제. grep으로 참조 없음 재확인 후 삭제, 테스트 스위트 전체 재실행해 영향 없음 확인.

## 2026-08-04 자연어 추천 — 막연한 요청에 되묻기(clarify) 기능 추가

- 사용자가 "배고파 밥먹고 싶어"처럼 아무 단서 없는 요청에도 후보 중 아무거나 3곳을 추천해버리는 게 문제라고 지적. 실제로 그럴듯하게 지어낸 이유("20첩 백반이나 고등어조림 백반은 푸짐하고...")까지 붙어 나와서 마치 의미 있는 추천처럼 보이는 게 더 문제였음.
- **설계 결정**: 새 Gemini 호출을 추가하지 않고, 기존 1회 호출(`pickRecommendations`) 안에서 모델이 스스로 "이 요청이 충분히 구체적인가"를 먼저 판단하게 함 — 지역/거리, 음식종류, 분위기·목적 중 최소 하나도 파악 안 되면 추천 대신 `{"type":"clarify","question":"..."}`로 되묻고, 그렇지 않으면 기존처럼 `{"type":"picks","items":[...]}`로 응답. 규칙 기반 키워드 사전으로 "막연함"을 판단하는 대안도 검토했으나, 자연어 표현이 워낙 다양해 사전이 금방 허술해질 것으로 보여 기각 — 이미 쓰고 있는 LLM 호출에 판단까지 얹는 게 호출 비용 안 늘고 더 유연함.
- **되묻기 1회 제한**: 무한 되묻기를 막기 위해, 사용자가 답변을 입력하면 프론트(`public/recommend.js`)가 "원래 질문 + 답변" 문자열을 이어붙여 `forcePicks:true`로 재요청. 서버는 `forcePicks`가 오면 프롬프트에서 clarify 옵션 자체를 빼서 모델이 무조건 추천하게 만듦. 모델이 지시를 무시하고 그래도 되물으면(방어적으로 대비) 추가 질문 없이 안내 메시지만 보여주고 종료 — 프론트 상태(`awaitingAnswer`)로 라운드를 추적하고, 서버는 대화 상태를 전혀 저장하지 않음(요청마다 독립적, 프론트가 문자열만 이어붙이는 무상태 설계).
- `lib/recommend.js`의 `parsePicksResponse(text, candidates)` → `parseRecommendResponse(text, candidates)`로 개편, 반환값이 배열에서 `{type, items|question}` 객체로 바뀜. 하위호환으로 모델이 예전처럼 배열만 반환해도 picks로 처리하도록 방어 로직 유지.
- **검증**: curl로 직접 "배고파 밥먹고 싶어" 호출 → `{"needsClarification":true,"question":"어떤 지역의 식당을 찾으시는지, 어떤 종류의 음식을 선호하시는지..."}` 확인, 자연스러운 질문이 생성됨. 다만 이후 무료 할당량이 다시 불안정해져서(하루 종일 반복된 RPM성 제한) 브라우저에서 "질문 → 답변 → 실제 추천"까지 이어지는 전체 흐름은 실제 Gemini 응답으로 끝까지 확인하지 못함 — 단위테스트(`parseRecommendResponse`의 clarify/picks/하위호환/오류 케이스 5개)와 코드 리뷰로만 검증된 상태. 사용자 요청으로 여기서 커밋·푸시하고 나중에 할당량이 안정되면 재확인 필요.

## 2026-08-05 추천받기 기능 개선점 점검 + 후보 선별 로직 1차 개선

- 사용자가 "추천받기 기능을 개선하고 싶다"고 요청해 현재 구현 상태를 코드/체크리스트/실데이터로 점검함. 단위테스트는 전부 통과하는 상태였고, 체크리스트상 남은 항목은 무료 할당량 소진으로 보류된 종단 간 검증 2건뿐이었음.
- 코드 검토 중 실데이터를 직접 세어 새로운 문제를 발견함: 전체 식당 1,135곳 중 `verified`(사람이 확인)는 99곳뿐인데, `lib/recommend.js`의 `buildCandidateList`는 origin(위치조건)이 없을 때 verified 우선 정렬 후 상위 80개만 잘라 Gemini에 넘기고 있었음. 즉 "매콤한 거 먹고 싶어"처럼 음식종류/분위기만 있고 위치조건이 없는 질의는 실질적으로 전체의 20% 미만(그것도 회차순 편향)만 놓고 추천이 이뤄지는 구조였음. 위치조건이 있는 질의는 반경 필터가 있어 이 문제가 상대적으로 적음.
- 사용자가 이 항목(①)부터 진행을 요청해 수정함: `extractRelevanceKeywords`(조사/필러 불용어 제외, 2글자 미만 제외)로 질의에서 키워드를 뽑고, `relevanceScore`로 후보의 name/menu/review/region/title과 매칭해 점수를 매김. `buildCandidateList`는 origin이 없을 때 이 점수(동점이면 기존처럼 verified) 순으로 정렬하도록 변경 — 키워드가 아예 안 뽑히면 기존 verified-우선 동작 그대로 유지되는 폴백 구조라 기존 테스트를 깨지 않음.
- Gemini 호출 횟수/비용은 그대로(로컬 계산만 추가), `api/recommend.js`가 `buildCandidateList` 호출 시 `query`를 넘기도록 한 줄만 변경. 단위테스트 4개 추가(관련도 우선순위 역전 케이스, 키워드 없을 때 폴백, 키워드 추출 규칙), 전체 스위트 재실행해 기존 테스트 포함 전부 통과 확인.
- **알아둘 점**: 불용어 목록이 형태소 분석 없이 손으로 짠 근사치라, "먹고싶다" 같은 활용형이나 목록에 없는 조사가 붙은 단어는 걸러지지 않고 그대로 키워드로 들어갈 수 있음(예: "해산물이"). 관련도 스코어링이라 오탐이 있어도 최악의 경우 기존 verified-우선 동작과 비슷한 수준으로 떨어질 뿐 크게 나빠지지는 않지만, 실사용 질의 패턴을 보고 불용어 목록을 다듬을 여지는 있음.
- 남은 개선 후보(사용자에게 이미 제안, 아직 미착수): review 80자 절단 재검토, GPS 기반 "현재 위치" 옵션, 되묻기/재추천 비율 등 운영 데이터 수집, 남은 종단 간 검증 2건.

## 2026-08-05 나머지 개선 항목(②~④) 진행

- ②(review 80자 절단): 실데이터를 세어보니 review 필드 최대 길이가 53자로, 80자 컷에 걸리는 사례가 0건이었음. 코드 검토 단계에서 "잘릴 수 있다"고 지적했던 건 실데이터 확인 없이 코드만 보고 짐작한 것이었음 — 실제로는 문제가 아니라 판단해 변경하지 않음. **교훈**: 데이터 길이/분포를 짐작하지 말고 실측 후 판단할 것.
- ④(GPS "현재 위치" 옵션): `public/index.html`에 위치 토글 버튼(`recommendLocationBtn`)과 상태 안내(`recommendLocationStatus`) 추가. `public/recommend.js`는 버튼 클릭 시 `navigator.geolocation.getCurrentPosition`으로 좌표를 받아 토글 상태로 유지하고, 이후 모든 추천 요청(`신규/되묻기 답변/다시 추천받기`)에 `originCoords`로 함께 실어 보냄. 권한 거부/미지원 브라우저는 안내 문구로 처리, alert 사용 안 함(기존 UI 패턴인 상태 텍스트 방식 재사용).
  - `api/recommend.js`는 `originCoords`가 유효하면 지명 지오코딩(`geocodeAddress`)을 건너뛰고 그 좌표를 바로 origin으로 사용, 응답의 `origin` 라벨은 "현재 위치"로 표시.
- GPS 기능을 구현하면서 파생된 별개 버그를 하나 더 발견함: "과천인데 조용한 곳" 처럼 **출발지는 있지만 이동시간 표현이 없는 문장**은 `intent.radiusMinutes`가 null이라 `radiusKm`도 null이 되어, 위치조건을 언급했는데도 거리 필터가 전혀 적용되지 않고 있었음(①에서 고친 키워드 관련도 정렬로만 처리됨). GPS 버튼도 시간 표현 없이 누르는 게 일반적인 사용 패턴이라 이 버그를 고치지 않으면 GPS 기능 자체가 무의미해짐 — `lib/recommend.js`에 `resolveRadiusMinutes`(기본값 60분)를 추가해 출발지가 있으면 항상 반경 필터가 걸리도록 수정. 단위테스트 추가, 기존 테스트 전부 통과 확인.
- ③(남은 종단 간 검증, 되묻기→답변→추천 실제 흐름): 이 세션 환경(bash·PowerShell 둘 다)에 `GEMINI_API_KEY`가 없음을 확인함(`.env.local`에도 없음) — 지난 세션 노트에 있던 "OS 환경변수에 이미 있던 키"가 이 세션에서는 안 잡힘(PC가 바뀌었거나 세션이 새로 시작되며 유실된 것으로 추정, 원인 미확인). 실제 Gemini 호출이 필요한 검증은 진행 못함 — 사용자가 키를 다시 넣어주거나 직접 브라우저로 테스트해야 함.
- 프론트 변경(GPS 버튼)은 이 환경에 브라우저 자동화 도구가 없어(Playwright 등 미탑재) 실제 클릭·위치권한 프롬프트·시각적 확인은 하지 못함. `node --check`로 문법 검증, 로컬 정적 서버(`python -m http.server`)로 새 DOM 요소·CSS·JS가 정상 서빙되는지만 확인함 — **사용자가 실제 브라우저에서 위치 버튼 동작을 확인해줘야 함**.

## 2026-08-05 GEMINI_API_KEY 재발급 + 종단 간 검증 완료

- 사용자가 새 Gemini API 키를 채팅으로 전달, `.env.local`에 `GEMINI_API_KEY`로 추가함(기존엔 아예 없었음). 직접 `generateContent` 호출로 키 유효성부터 확인(200 응답).
- `npx vercel dev --listen 8000`으로 로컬 서버를 띄우고 Node fetch 스크립트로 `/api/recommend`를 5개 시나리오 실제 호출해 이번 세션과 지난 세션들에서 미뤄뒀던 종단 간 검증을 전부 마쳤음:
  1. "배고파 밥먹고 싶어" → `needsClarification: true` (12단계 되묻기 기능)
  2. 위 질문에 답변을 이어붙여 `forcePicks:true`로 재요청 → picks 3곳 정상 수신 (되묻기 1회 제한 흐름)
  3. "해산물 먹고 싶어"(위치조건 없음) → 실제로 해산물 전문점 3곳(홍콩 해산물 레스토랑, 백합식당, 게장 전문점)이 나옴 — verified 편향이 있던 예전 로직이었다면 절대 나오지 않았을 조합이라, 13단계에서 만든 관련도 기반 정렬(①)이 실데이터에서도 의도대로 작동함을 확인
  4. "과천에서 조용하게 한잔 할 수 있는 곳"(시간 표현 없음) → `radius_km: 48`(기본 60분 반경 적용), 결과가 실제로 과천에서 7km 이내 — `resolveRadiusMinutes` 수정이 실데이터에서도 작동함을 확인
  5. GPS 좌표(서울시청 부근)로 요청 → `origin: "현재 위치"`, `radius_km: 48`, 결과가 실제로 0.1~1.4km 반경 이내 — GPS 옵션(④) end-to-end 확인
  - exclude(다시 추천받기)도 별도로 확인, 이전 3곳을 제외 목록으로 보내니 겹치지 않는 새 3곳(334/68/341회)이 나옴
- 검증 후 `vercel dev` 프로세스는 종료함. checklist.md의 12단계·13단계 미검증 항목을 모두 완료로 갱신.
- 남은 미검증은 딱 하나: GPS "내 위치" 버튼을 실제로 클릭했을 때 브라우저 위치권한 프롬프트가 뜨고 정상 동작하는지는 이 환경에 브라우저 자동화 도구가 없어 확인 못함(API 자체는 좌표를 직접 보내는 방식으로 검증 완료, 프론트 클릭 UX만 미검증) — 사용자가 직접 확인 필요.
- **환경 참고**: `$TMPDIR`가 이 bash 환경에서 비어있어 `"$TMPDIR/foo.log"`가 `/foo.log`(루트, 권한 없음)로 풀려버림 — 이 프로젝트에서 임시 로그 파일을 쓸 땐 세션 스크래치패드 경로를 명시적으로 써야 함. 또한 `cmd1 && cmd2 && cmd3 &` 형태는 전체 체인이 서브셸로 백그라운드 실행되어, 그 안에서 설정한 변수가 같은 명령의 다음 줄(부모 셸)에는 안 보임에 유의.

## 2026-08-05 사용자 실사용 리포트 — 출발지 오인식 + GPS 우선순위 버그

- 사용자가 배포 사이트에서 GPS 기능을 직접 써보고 리포트: "내 위치"(과천)를 누른 뒤 "친구들과 저녁식사와 술한잔을... 친구들은 위례, 서울 강남이 집이라 셋이 모이기 편한곳으로"를 입력했더니 결과가 전부 강남 근처만 나왔다고 함. 3명(본인+친구 둘)의 위치를 종합 고려했는지 궁금하다는 질문.
- 재현해보니 실제로는 GPS가 실패했음(화면에 "위치를 확인하지 못했습니다" 안내가 떠 있었고 버튼도 비활성 상태로 남아있었음 — 스크린샷으로 확인). GPS가 실패하니 서버가 텍스트에서 위치를 다시 추출했는데, `extractIntentLocal`이 "강남"(친구 거주지를 설명하는 3인칭 문맥)을 그대로 "출발지"로 오인식해 강남 반경으로만 필터링한 것이 원인. "위례"는애초에 지명 사전에 없어 인식조차 안 됐음. 여러 사람 위치를 종합하는 기능 자체가 원래 없었다는 점도 함께 설명함.
- 사용자가 GPS 실패 원인 확인을 요청해 "어느 주소로 테스트했는지" 물었더니 "배포된 사이트(foodtrip-six.vercel.app, https)"라고 답함 — secure-context 문제는 아니므로 브라우저 위치 권한 거부나 OS 위치 서비스 비활성일 가능성이 높음(직접 재현 불가, 사용자 브라우저 환경이라 이 세션에서는 확인 못함).
- 사용자가 추가로 "GPS(과천)를 찍은 채로 텍스트에 '지금 광주인데'라고 다른 위치를 명시하면 어떻게 되냐"는 케이스를 직접 제시함 — 코드 추적 결과 기존 로직은 GPS가 있으면 `else if` 분기라 텍스트를 아예 안 보고 조용히 GPS만 씀(광주 언급이 버려짐, 알림도 없음). 이것도 "강남 오인식"과 같은 뿌리(텍스트 속 지명이 누구 위치인지 구분 못 함)에서 나온 문제임을 확인.
- 사용자가 우선순위를 확정함: **1인칭으로 명시된 텍스트 위치 > GPS > 그 외 텍스트 추정 위치.** 사용자가 방금 텍스트로 명시한 것은 GPS(이전에 눌러둔 상태일 수 있음)보다 더 최근·명확한 의사표현이라는 논리.
- 구현: `lib/koreanRegions.js`에 위치 정보(index/endIndex)를 포함해 매칭 결과를 반환하는 `findAllRegionMatches` 추가(기존 `findRegionInText`는 내부적으로 후보 수집 로직을 공유하도록만 리팩터링, 반환값/동작은 완전히 동일 — `api/lookup.js`가 이미 쓰고 있어서 건드리면 안 됐음). `lib/recommend.js`에 `extractOwnOrigin` 추가: 지명 바로 앞 절(가장 가까운 문장부호 이전 구간)에서 1인칭 표현과 3인칭 표현 중 어느 쪽이 지명에 더 가깝게(나중에) 나오는지로 문맥을 판단 — "친구는 강남에 살고, 나는 지금 과천이야"처럼 한 문장에 둘 다 있어도 각 지명이 어느 화자에 속하는지 올바르게 구분됨.
- 1인칭 표현 목록에 "지금"을 포함시킴 — "지금 광주인데"처럼 대명사 없이도 매우 흔한 자기 위치 표현이라 실전 커버리지에 중요했음. 3인칭 목록은 친구/가족/동생/애들 등 흔한 호칭 + 주어 조사(은/는/이/가) 조합만 인정해 오탐 위험을 낮춤.
- 3인칭 언급만 있고 1인칭 신호가 전혀 없으면 **origin: null(모름)** 로 처리하도록 함 — 억지로 아무 지명이나 골라서 틀리는 것보다, "지역조건 없음"으로 처리해 키워드 관련도 기반으로만 추천하는 게 안전하다는 기존 프로젝트 원칙("region.py"에서 확립된 "틀린 확신보다 모름이 낫다")을 그대로 적용함.
- `vercel dev` + 실제 호출로 3가지 케이스 전부 재검증: 강남 오인식 문장 → origin null(서울 각지 분산 결과), GPS+텍스트 충돌("지금 광주인데") → 텍스트(광주) 우선 확인, GPS만 있는 경우 → 여전히 정상 동작. 단위테스트 5개 추가, 전체 통과.
- **알아둘 점**: 이 휴리스틱은 정규식 기반이라 완벽하지 않음. 같은 지명이 문장에 두 번 나오면(`findAllRegionMatches`도 `findRegionInText`처럼 각 지명 사전 항목의 "마지막 등장 위치"만 봄) 앞쪽 등장의 문맥은 무시됨 — 예: "지금 나는 과천인데 ...(문장부호 있는 긴 설명)... 과천에서 이동거리 1시간 이내로"처럼 같은 지명이 문장부호를 사이에 두고 두 번 나오면 두 번째 등장의 문맥(화자 표현 없음)만 보게 됨. GPS와 충돌하지 않는 한 결과는 동일해 실질적 문제는 없지만, 알아둘 한계로 기록해둠.

## 2026-08-05 동명이지역("광주") 되묻기 추가

- 위 GPS(과천)+텍스트("지금 광주인데") 충돌 검증 결과를 보여줬더니, 사용자가 "광주는 광주광역시도 있지만 경기도 광주시도 있는데, 지명이 동일해서 헷갈리면 재질문해야지"라고 지적함. 실제로 사전(`lib/koreanRegions.js`)에는 이미 "경기도 광주"/"경기 광주"라는 조합 키가 별도로 존재해 이 충돌이 예전부터 알려져 있었음(주석에 "동명 지역은 더 흔히 쓰이는 쪽으로 1개만 등록"이라고 적혀 있었음) — 지금까지는 그냥 "더 흔한 쪽"(광주광역시)으로 조용히 확정해버리는 방식이었는데, 추천 기능처럼 결과에 직접 영향을 주는 곳에서는 이게 위험하다는 지적.
- "고성"도 같은 패턴(강원도 고성군 vs 경남 고성군, "경남 고성"/"경상남도 고성" 조합 키가 이미 존재)이라, 하드코딩 대신 사전 자체를 스캔해서 자동으로 동명이지역을 찾아내는 방식으로 구현하기로 함 — `lib/koreanRegions.js`의 `ambiguousRegionsFor`: "{시도} {짧은이름}"처럼 공백 포함 조합 키가 있고, 그 짧은이름이 단독 키로도 등록돼 있는데 서로 다른 지역을 가리키면 동명이지역으로 자동 판정. 이렇게 하면 나중에 사전에 같은 패턴의 조합 키가 추가돼도 코드 수정 없이 자동으로 되묻기 대상이 됨.
- 구현 중 놓치기 쉬운 함정을 하나 확인함: "남양주"(경기도 남양주시)는 "양주"(경기도 양주시)를 문자열로 포함하지만 이건 동명이지역이 아니라 그냥 다른 지명이 우연히 부분 포함관계인 것뿐임(공백 없이 붙어있는 고유명사). `ambiguousRegionsFor`는 "공백이 있는 조합 키"만 대상으로 삼아서 이런 오탐을 피함 — `node -e`로 직접 두 경우를 비교 출력해 실제로 "광주"/"고성"만 걸리고 "양주"는 안 걸림을 확인.
- `extractOwnOrigin`이 최종 채택한 지명(1인칭이든 폴백이든)이 동명이지역이면 origin을 확정하지 않고 `{origin:null, ambiguous:{name, options}}`를 반환하도록 함. `api/recommend.js`는 이 신호를 받으면 Supabase 조회나 Gemini 호출 전에 즉시 `needsClarification`으로 응답 — 기존에 이미 있던 "막연한 질의 되묻기" UI/흐름(프론트 `public/recommend.js`)을 그대로 재사용해서 프론트 변경은 전혀 없었음.
- `forcePicks`(되묻기에 이미 한 번 답한 재요청)에서 같은 동명이지역 질문을 무한 반복하지 않도록 `allowAmbiguous:true`를 넘겨 그 경우엔 "더 흔한 쪽"(기존 동작과 동일한 기본 해석)으로 조용히 확정하게 함 — 사용자가 "경기도 광주"처럼 구체적으로 답하면 애초에 동명이지역 판정 자체가 안 걸려서(조합 키가 직접 매칭) 정상적으로 원하는 지역이 나오고, 혹시 답변이 여전히 모호하더라도 무한 루프 없이 기본값으로 넘어감.
- `vercel dev` + 실제 Gemini 호출로 3단계 흐름 전부 검증: "지금 광주인데" → clarify(Gemini/DB 호출 전에 즉시 응답이라 quota 소모 없음) → "경기도 광주요" 이어붙여 forcePicks 재요청 → 실제로 경기도 광주시 소재 식당들(101회 "경기도 광주 나들이 밥상" 등)만 정상 추천 → 처음부터 "지금 경기도 광주인데"로 물으면 되묻지 않고 바로 같은 결과.
- 단위테스트 4개 추가(기존 "1인칭 명시" 테스트가 우연히 "광주"를 예시로 썼던 걸 "전주"로 교체, 동명이지역 되묻기 신호, 구체적 표현 시 즉시 확정, allowAmbiguous 폴백). 전체 lib/recommend.js 25개 테스트 통과.

## 2026-08-05 되묻기 답변 입력창 부재 버그 수정

- 배포 반영 후 사용자가 실제로 "광주" 동명이지역 되묻기 화면을 받았는데, 답을 입력할 곳을 못 찾겠다고 리포트("이걸 답할 창이 없네. 기존 prompt창은 편집이 불가한 상태"). 코드상으로는 위쪽 검색창(`recommendInput`)이 실제로 비활성화되는 지점이 없어 순수 코드 버그는 아니었던 것으로 보이고, 되묻기 시 그 칸을 비워서(`value=''`) 재사용하도록 만들었던 게 문제로 추정됨 — 빈 칸에 예시 placeholder("예: 지금 나는 과천인데...")만 보여서 사용자가 "여기 입력하면 된다"는 걸 알아채기 어려웠을 것.
- 이 환경엔 브라우저 자동화 도구가 없어 실제 클릭/타이핑을 재현해 진짜 원인(코드 버그 vs UX 인지 문제)을 확정할 수 없었음. 다만 원인이 무엇이든 되묻기 질문 바로 아래 전용 답변 입력창을 만들면 확실히 해결되는 문제라 판단해 그 방향으로 수정.
- `public/recommend.js`의 `renderClarifyQuestion`이 질문 텍스트와 함께 별도 폼(`#recommendClarifyForm` + `#recommendClarifyInput` + "답변하기" 버튼)을 렌더링하도록 변경. `recommendResult`에 `submit` 이벤트 리스너를 위임 방식으로 추가해 동적으로 생성된 폼의 제출을 처리(`submit` 이벤트는 버블링되므로 위임 가능). 답변 처리 로직(`원래 질문+답변 이어붙여 forcePicks:true로 재요청`)은 `submitClarifyAnswer` 함수로 추출해 기존 위쪽 검색창 경로와 새 전용 입력창 경로 둘 다에서 재사용 — 위쪽 검색창도 여전히 동작하도록 보조 경로로 남겨둠(혹시 그게 원래 문제없이 동작했을 경우를 대비, 제거하지 않음).
- 부수적으로 발견한 사소한 문제도 같이 고침: `autoGrowRecommendInput`이 입력한 만큼 textarea 높이를 늘리는데, 프로그램적으로 `value=''`로 비울 때는 `input` 이벤트가 안 붙어서 높이가 리셋 안 되고 이전 긴 질문만큼 커진 채로 남아있었음(빈 칸인데 크게 보이는 시각적 문제). `resetRecommendInput()` 헬퍼로 값과 높이를 함께 초기화하도록 통합.
- 이 수정은 프론트엔드 전용이라 `npm test`에는 영향 없음(25/25 그대로 통과 확인). 실제 클릭 상호작용은 여전히 이 환경에서 검증 불가 — 사용자가 다시 확인해줘야 함.

## 2026-08-05 되묻기 답변 이어붙이기 시 문장 절 오염 버그

- 새 전용 답변 입력창으로 "경기도 광주야"라고 답했는데 "조건에 맞는 식당을 찾지 못했습니다"가 나온다는 리포트. `extractOwnOrigin`을 직접 재현해보니 origin이 null로 나옴 — 원인은 `submitClarifyAnswer`가 `${lastQuery} ${typed}`처럼 공백만으로 이어붙여서, 답변 문장이 원래 질문 끝부분("친구들은 위례와 서울 강남이 집이라 셋이 모이기 편한곳으로")과 마침표 없이 하나로 이어짐. `classifySpeaker`는 가장 가까운 문장부호(.!?개행) 이후를 하나의 "절"로 보고 그 안에서 1인칭/3인칭 마커 중 지명에 더 가까운 쪽을 판단하는데, 이 경우 절 전체가 "...친구들은... 경기도 광주야"까지 이어져서 "친구들은"이 "경기도 광주"보다 더 가까운(나중에 나온) 마커로 인식돼 3인칭으로 오분류됨.
- 추가로 재확인한 사실: `findAllRegionMatches`(및 기존 `findRegionInText`)는 같은 지명이 텍스트에 여러 번 나와도 `lastIndexOf` 기준 마지막 등장만 본다. 그래서 "나는 광주고"(1인칭, 앞쪽)의 "광주"는 애초에 매칭 대상에서 아예 안 보이고, "경기도 광주야"(뒤쪽, 마지막 등장)만 보게 됨 — 이 자체는 기존에 "드문 케이스"로 문서화해뒀던 한계인데, 되묻기 답변 이어붙이기 흐름에서 실제로 발생함을 이번에 확인함.
- 근본 수정은 이 흐름에서 매번 발생하는 "절 경계가 없다"는 문제를 겨냥해, `submitClarifyAnswer`가 원래 질문과 답변 사이에 마침표를 넣어 이어붙이도록 변경(`${lastQuery}. ${typed}`) — 이렇게 하면 답변이 항상 자기 자신만의 절로 분리되어 앞 문장의 3인칭 문맥에 오염되지 않음. `lib/koreanRegions.js`의 "마지막 등장만 본다"는 근본 한계 자체는 건드리지 않았음(이 흐름만 고치면 충분하고, 매칭 엔진을 다중 등장 지원으로 바꾸는 건 지금 트리거되는 범위 밖이라 과잉수정으로 판단).
- 재발 방지 단위테스트 추가(`test/recommend.test.js`, 마침표로 절이 끊긴 상태에서 뒤 절 지명이 앞 절 3인칭 문맥과 안 섞이는지 확인). `vercel dev`로 정확히 사용자가 겪은 시나리오(원문+"경기도 광주야" 답변)를 실제 재현해 정상 추천됨을 확인.

## 2026-08-05 전체 데이터 품질 점검 및 정리

- 사용자가 직접 데이터를 편집 입력한 뒤 "전체적으로 점검해줘"라고 요청 — 코드가 아니라 데이터 자체의 정합성 점검이라, 로컬 vercel dev(운영 Supabase에 연결됨)를 통해 8가지 관점의 읽기 전용 스캔 스크립트를 스크래치패드에 작성해 실행함(레포에는 커밋 안 함, 1회성 점검 도구).
- 발견한 문제들을 전부 리스트로 보여주고 사용자 판단을 구한 뒤에만 수정함 — "기존 등록 식당은 손대지 않는다", "틀린 확신보다 모름이 낫다" 원칙을 그대로 적용, 애매한 케이스(50회 고기리막국수)는 사용자가 직접 조사 중이라고 해서 그대로 둠.
- 좌표 채우기는 새 로직을 만들지 않고 `api/episodes/[id].js`의 기존 PUT 핸들러(좌표 없는 항목만 자동 지오코딩)를 그대로 재사용 — GET으로 현재 restaurants/verified를 받아온 뒤 그대로 PUT만 다시 보내는 방식. 이렇게 하면 실제 배포 코드와 100% 같은 경로를 타므로 별도 스크립트가 지오코딩 로직을 중복 구현할 필요가 없었음.
- `restaurants_source_url` 필드는 PUT API가 아예 다루지 않는 필드라는 걸 발견함(`restaurants`, `verified`만 업데이트) — 157회의 잔재 필드를 지우려면 API로는 불가능해서, `lib/supabase.js`의 `getSupabase()`를 그대로 가져와 서비스롤 키로 직접 Supabase에 연결하는 1회성 스크립트를 작성함. 안전장치로 `restaurants`가 비어있는지 먼저 확인하고, 비어있을 때만 진행하도록 함(혹시라도 실제 데이터가 있는 회차를 잘못 건드리지 않기 위함).
- 중복 등록 3쌍 중 2쌍(잡어와묵은지, 수안보대장군)은 제거 전 반드시 같은 회차의 "나머지 식당 목록"을 먼저 조회해 확인함 — 회차 전체를 덮어쓰는 게 아니라 배열에서 해당 식당 1곳만 필터링해서 제거하는 방식으로, 같은 회차의 다른 정상 식당(35회 7곳, 63회 5곳)은 전혀 건드리지 않음.
- 이 작업은 순수 데이터 정리(Supabase 직접 수정)라 레포 코드 변경이 없음 — git 커밋 대상 아님. checklist.md에만 기록.

## 2026-08-06 여러 사람 위치 종합 중간지점 찾기

- 17단계에서 "다음 과제로만 등록, 착수 안 함" 상태였던 기능. 착수 전 사용자에게 입력 방식을 물음 — 위치를 각각 명시 입력받는 UI(정확하지만 UX 번거로움) vs 기존 자연어 문장 파싱 확장(자연스럽지만 이 기능은 이미 강남 오인식·GPS 우선순위·동명이지역 등 여러 번 사고가 났던 영역). 사용자가 "자연어 문장 확장"을 선택 — 파싱 리스크를 감수하기로 함.
- 설계: 기존 `extractOwnOrigin`(1인칭만 채택, 3인칭 제외 — "내 위치 1곳" 전용)과는 별도로 `extractMeetupOrigins`를 신설. 이유: 다인원 모드에서는 3인칭("친구는 ~")도 참가자 위치로 써야 하는데, 기존 함수의 "3인칭 제외" 로직을 조건부로 뒤집으면 단일 출발지 흐름(수차례 사고를 겪으며 다듬어진 검증된 로직)까지 건드릴 위험이 있었음. 별도 함수로 분리해 기존 로직은 전혀 손대지 않음.
- "지명이 여러 개 나오면 자동으로 여러 명 모임"으로 해석하면 오작동 위험이 큼(예: "강남에서 홍대까지 가는 길에 있는 맛집"은 경로 설명이지 모임이 아님). 그래서 "모이/만나/중간지점/가운데" 등 만남 표현 키워드 + 서로 다른 지명 2곳 이상, 이중 조건이 모두 맞을 때만 동작하도록 게이트를 걸었음. 키워드 하나만으로는 오탐 여지가 있다고 보고(예: "강남에서 친구를 만나서 홍대 얘기 들었는데 홍대 맛집 추천해줘") 지명 2곳 이상 조건을 반드시 같이 요구.
- 반경 계산: 참가자 지명을 전부 지오코딩 → 무게중심(위경도 단순 평균) → 그 중심에서 각 참가자까지의 최대거리를 반경으로 사용. 참가자들이 멀리 흩어져 있으면 반경도 자동으로 넓어지고, 가까이 모여 있으면 좁아짐 — 별도 설정 없이 참가자 분포에서 자연스럽게 도출.
- **실제 버그 발견 및 수정**: `vercel dev`로 "나는 성남이고 친구는 광주에 살아서..." → 동명이지역("광주") 되묻기 → 프론트가 원문 뒤에 "경기도 광주요"를 이어붙여 재요청(기존 되묻기 흐름 그대로 재사용)하는 시나리오를 검증하다가, 원문에 남아있는 애매한 "광주"와 답변의 구체적인 "경기도 광주"가 서로 다른 지역(광주광역시 vs 경기도 광주시)으로 각각 채택되어 참가자가 2명인데 3곳(성남·경기도 광주·광주)으로 잘못 잡히는 문제를 발견함. `extractMeetupOrigins`에 "더 긴 지명이 이미 채택되면 그 안에 포함되는 짧은 지명은 같은 사람으로 보고 중복 채택하지 않는다"는 로직을 추가해 수정(이름 길이 내림차순으로 훑으며 이미 채택된 이름에 포함되는 짧은 이름은 건너뜀). 재발 방지 회귀테스트 추가.
- 검증 중 별개로 발견한 환경 문제(애플리케이션 버그 아님): Windows Git Bash에서 `curl -d '{"query":"한글..."}'` 형태로 요청하면 한글 인자가 깨진 인코딩으로 서버에 전달됨(터미널 표시 문제가 아니라 실제 바이트 손상 — `console.error`로 서버가 실제로 받은 문자열을 찍어서 확인). 이후 검증은 Node `fetch` 스크립트로 전환. 이 프로젝트에서 향후 vercel dev를 curl로 실제 한글 입력 검증할 때는 Git Bash curl 대신 Node fetch를 쓸 것.
- `npm test`(lib/recommend.js 32개, 전체 스위트 포함 회귀 없음) + `vercel dev` 3개 시나리오(3인 중간지점 성공, 동명이지역 되묻기→확정 성공, 기존 단일 출발지 문장 무영향) 실제 검증 완료. 프론트(`public/recommend.js`) 코드 변경 없음 — 서버가 `notice` 필드에 "○○·○○ 중간지점 기준으로 찾았습니다"를 담아 보내고, 프론트는 기존에 있던 notice 렌더링 경로를 그대로 재사용.
