# 백반기행 회차별 정리 웹페이지 구축계획

## 0. 배경 및 결정사항

`project.md`의 크롤러 스켈레톤을 실제로 완성하고, 수집한 데이터를 회차별로 보여주는 웹페이지를 만든다.
실제 대상 사이트(TV조선 "백반일기" 게시판)를 직접 확인해 아래 4가지를 사용자와 함께 확정했다.

| 항목 | 결정 |
|---|---|
| 웹페이지 기술 | 프론트는 정적 HTML/CSS/JS + Vercel Serverless Functions(API) — *2026-07-30 변경, 아래 0-1 참고* |
| 상세 콘텐츠 표시 | 본문은 원문 그대로 표시 + 식당명/주소는 AI 추출 필드를 별도로 얹음 (완전 자동 신뢰 안 함, 수동 보정 전제) |
| 지도 연결 | 네이버맵 검색 링크 `https://map.naver.com/p/search/{식당명 주소}` |
| 이미지 처리 | `img.tvchosun.com` 원본 URL 그대로 참조 (다운로드 안 함) |
| 식당 정보 편집 | 배포된 사이트에서 직접 수정/추가 (관리자 인증 필요 → DB 필요) |
| 배포 | GitHub 저장소 → Vercel 배포 (Serverless Functions 포함) |

### 0-1. 2026-07-30 변경사항 (디자인 드래프트 검토 중 추가된 요구사항)

디자인 드래프트 검토 중 "회차 카드에 방문 식당명/주소를 표시하고 네이버맵으로 연결" 요청이 추가되었고, 이를 위해 두 가지가 확정되었다.
1. 지도 링크는 좌표가 아니라 **식당명+주소 검색 링크**로 구성한다.
2. 식당명/주소는 **AI(본문 텍스트 + 필요시 웹검색)로 1차 추출**하고, **배포된 사이트에서 직접 수정/추가할 수 있어야** 한다.

(2)는 "서버 없는 정적 사이트"라는 기존 결정과 정면으로 배치된다. 배포 후 실시간 편집이 저장되려면 데이터를 어딘가 영속시켜야 하므로, DB + 인증 있는 API가 필요해졌다. 이에 따라 아키텍처를 아래와 같이 확장한다 (섹션 2, 2-4 참고). 이 변경으로 개발 범위가 "정적 파일 하나"에서 "DB·서버리스 API·관리자 인증·식당 정보 수집 파이프라인"으로 늘어난다는 점을 인지하고 있어야 한다.

### 0-2. 2026-07-30 실제 구현 현황

계획서상 항목들을 실제로 구현·실행한 결과는 다음과 같다 (자세한 경과는 context-notes.md 참고).

| 항목 | 상태 |
|---|---|
| 크롤러 (목록 18p + 상세 352건) | 완료 — 결번/중복 없음, 본문 추출 실패 0건 |
| 지역 추정 (`crawler/regions.py`) | 완료 — 제목 텍스트만 사전 매칭 (본문까지 검색하면 오탐 심각해 제목만 쓰기로 변경) |
| **식당 정보 수집** (`crawler/naver_place.py`, `enrich_naver.py`) | 완료 — 당초 "AI 추출"로 계획했던 부분을 **네이버 블로그의 "장소 첨부" 위젯 파싱**으로 구현. 정규식/LLM 추측보다 정확도 높음(상호명·주소·전화·좌표·placeId를 블로거가 이미 구조화해둔 데이터). 352건 중 220건(482곳) 확보 |
| 데이터 모델 | `restaurant_name`(단일) → `restaurants`(배열)로 변경 — 회차당 식당이 보통 2~4곳이라 배열 필요 |
| 정적 프론트엔드 | 완료 — 목록/검색/정렬/상세/편집모드 UI, Playwright로 데스크톱·모바일 실동작 검증 |
| GitHub 저장소 | 완료 — `ybkwon68-web/foodtrip` main 브랜치에 push 완료 |
| Supabase DB / 편집 API / Vercel 배포 | 미착수 — 사용자 계정 필요, 아래 섹션 2 계획 그대로 유효 |

식당 정보 수집이 "AI 추출"에서 "네이버 블로그 스크래핑"으로 바뀐 이유: 실제 본문(TV조선 방송 소개글)에는 상호명이 거의 안 나오고(예: "강동구 버섯전문점"처럼 업종·지역만 언급) 지역명만 있는 경우가 많아, 본문만으로는 AI를 쓰더라도 추출할 정보 자체가 없었음. 반면 네이버에 "허영만의 백반기행 {회차}회"로 검색하면 나오는 개인 블로거의 리뷰 글에는 실제 방문한 식당명이 있고, 그중 네이버 "장소 첨부" 기능을 쓴 글은 상호명/주소/좌표가 구조화된 형태로 그대로 들어있어 이쪽이 훨씬 신뢰도 높은 소스였음.

## 1. 대상 사이트 구조 분석 (curl로 실제 HTML 확인 완료)

### 목록 페이지
```
https://broadcast.tvchosun.com/broadcast/program/3/C201900033/bbs/8667/C201900033_10/list.cstv?search_text=&pg={1~18}
```
- 페이지당 20개 항목, 총 18페이지 (마지막 페이지는 20개 미만일 수 있음)
- 항목 셀렉터: `ul.item-list.col-4.wrap > li > a.vd-link`
  - `href` → 상세페이지 상대경로 (예: `/broadcast/.../714316.cstv?search_text=`)
  - `.thumb-box img` → 썸네일 URL
  - `.info-box p.title` → `"352회 끌어올려~ 텐션 만렙 김호영의 맛의 무대"` (회차번호+제목 혼합 텍스트)
  - `.info-box p.date` → `"2026.06.14"`

### 상세 페이지
```
https://broadcast.tvchosun.com{목록에서 얻은 href}
```
- `.board-view .title-box h3.tit` → 전체 제목 (목록과 동일)
- `.board-view .title-box .info span` → 작성일 (두 번째 span)
- `.cont-box` → **본문 HTML 원문** (`<p>`, `<b>`, `<span>` 스타일 태그와 `<img>`가 뒤섞인 자유 서술형 글)
  - 실제 확인 결과 식당명/지역/메뉴가 표나 필드로 정리되어 있지 않고, 허영만 화백 시점의 에세이 형태 텍스트임 → **구조화 파싱을 시도하지 않고 원문을 그대로 저장/표시**하기로 결정

## 2. 아키텍처

```
[크롤러: Python, 1회성 실행]
   목록 18페이지 순회 → 상세 페이지 개별 접속
        ↓
[AI 추출 단계] 본문 텍스트(+필요시 웹검색)로 식당명/주소 후보 추출 (신뢰도 낮음, 검수 전제)
        ↓
[seed/episodes.json]  (최초 적재용 스냅샷 — 회차정보 + 본문 + 식당명/주소 추정값)
        ↓ 최초 1회 적재
[DB: Supabase(Postgres)]  episodes 테이블 — 위 필드 + 수정이력
        ↑↓
[Vercel Serverless API (/api)]
   GET  /api/episodes        목록 조회
   GET  /api/episodes/:id    상세 조회
   PUT  /api/episodes/:id    식당명/주소 수정 (관리자 비밀번호 인증)
        ↑↓
[프론트엔드: HTML/CSS/JS]  목록/검색/정렬/상세 + "편집" 모드
        ↓
[GitHub 저장소] → [Vercel 배포] (Serverless Functions + 환경변수로 DB 접속정보/관리자 비밀번호 관리)
```

DB는 **Supabase(Postgres, 무료 티어)** 를 제안한다. 이유: 회차 수가 360여 개로 소규모라 어떤 DB든 충분하지만, Supabase는 Vercel과 자주 함께 쓰이고 REST API가 기본 제공되어 별도 ORM 없이 서버리스 함수에서 바로 호출 가능하다. 관리자 인증은 별도 회원 시스템 없이 **환경변수에 저장한 공용 비밀번호 하나**로 충분하다고 본다(개인 프로젝트 규모).

### 2-1. 크롤러 (`crawler/crawl.py`, project.md 완성)
- `requests` + `BeautifulSoup`, 정적 페이지로 확인되어 Selenium 불필요
- User-Agent 헤더 추가 (일부 서버의 기본 차단 회피)
- 요청 사이 `time.sleep(1)` 유지 (서버 부하 방지, project.md 원칙 계승)
- 1단계: 목록 18페이지 순회 → (회차 원제목, 방송일, 상세링크, 썸네일) 리스트 수집
- 2단계: 수집된 상세링크마다 접속 → `.cont-box` 본문 HTML 추출
- 회차번호는 제목 텍스트에서 정규식 `^(\d+)\s*회\s*(.*)`로 분리 (실패 시 로그 남기고 원제목 그대로 보존 — 수동 보정 가능하도록)
- 최종 결과를 `data/episodes.json`으로 저장 (CSV는 검수용으로 병행 저장 가능)

### 2-2. 정적 웹페이지 (`public/`)
- `index.html`: 단일 페이지, JS로 목록/상세 뷰 전환 (URL 해시 `#/episode/352` 방식으로 상세 접근)
- 목록 뷰: 카드형(썸네일 + 회차번호 + 제목 + 방송일), 텍스트 검색(제목 기준), 최신순/회차순 정렬
- 상세 뷰: 원문 본문 HTML 렌더링 (`.cont-box` 내용을 그대로 삽입하되 최소 sanitize 처리 — 원본 방송사 콘텐츠지만 방어적으로 스크립트성 태그는 제거)
- 반응형 카드 레이아웃 (모바일/데스크톱)

### 2-3. 식당 정보 수집 파이프라인 (구현 완료)
- `crawler/naver_place.py`: "허영만의 백반기행 {회차}회"로 네이버 블로그 검색(결과 페이지 직접 스크래핑) → 후보 글마다 `blog.naver.com/PostView.naver?blogId=...&logNo=...`에 접속해 본문 확인
  - 회차 매칭 검증: og:title에 "{회차}회"가 있으면 확정, 없으면 방송 제목의 키워드(지역명 등)가 겹치는지로 폴백 판단 (오래된 회차일수록 블로그 제목에 회차번호가 재언급 안 되는 경우가 많아서 추가한 로직)
  - 본문 HTML의 `data-module='{"type":"v2_map",...}'` 패턴(네이버 "장소 첨부" 위젯)을 파싱해 상호명/주소/전화/좌표/placeId 추출
- `crawler/enrich_naver.py`: 전체 회차를 순회하며 위 로직 실행, `restaurants` 배열과 근거 URL(`restaurants_source_url`)을 `seed/episodes.json`에 기록. 20건마다 중간 저장(중단되어도 유실 최소화)
- 결과는 항상 `verified: false`(미검수) 상태로 저장 — 자동 매칭이라 100% 확신할 수 없음. 근거 블로그 URL을 함께 저장해 관리자가 검수 시 바로 확인 가능
- **실행 결과**: 352건 중 220건(482곳) 확보(약 62%). 최근 회차일수록 성공률 높고, 오래된 회차(대략 150회 이전)는 관련 블로그 자체가 적어 매칭 실패 다수
- **리스크**: 네이버 검색결과 페이지는 공식 API가 아니라 직접 스크래핑 방식 — 페이지 구조가 바뀌면 깨질 수 있고, 대량/고빈도 요청 시 네이버 정책상 제약 가능성 있음. 공식 검색 오픈API(블로그 검색)로 교체하는 게 더 안정적이나, 이번엔 API 키 발급을 기다리지 않고 바로 진행하기로 사용자가 결정함

### 2-4. 편집 API 및 인증 (구현 완료, 실제 Supabase 연결만 남음)
- `supabase/migrations/0001_init.sql`: `episodes` 테이블 스키마 (크롤링 필드 + `restaurants` jsonb + `verified` + `updated_at` 자동 갱신 트리거 + RLS 활성화)
- `lib/auth.js`: 세션 저장소 없이 HMAC 서명으로 토큰 발급/검증 (`SESSION_SECRET` 환경변수로 서명, 12시간 만료). 단위 테스트로 정상/변조/만료/오시크릿 케이스 확인함
- `api/auth.js`: `POST` — 비밀번호(`ADMIN_PASSWORD` 환경변수)를 확인하고 토큰 발급
- `api/episodes.js`: `GET` — 전체 목록 조회
- `api/episodes/[id].js`: `GET` 단건 조회, `PUT` — `Authorization: Bearer <token>` 검증 후 `restaurants`/`verified` 갱신
- `crawler/seed_supabase.py`: `seed/episodes.json`을 Supabase에 최초 적재(upsert)하는 1회성 스크립트
- 프론트엔드(`public/script.js`)는 `/api/episodes`를 우선 호출하고, 실패하면(로컬 정적 서버 등 API가 없는 환경) `data/episodes.json` 정적 스냅샷으로 자동 폴백함. 편집 모드 로그인·저장도 실제 `/api/auth`, `/api/episodes/:id` PUT을 호출하도록 연결함
- 식당 수정 폼에는 전화/좌표 입력칸이 없어서, 주소를 바꾸지 않으면 기존 전화/좌표/placeId를 그대로 보존하고 주소를 바꿀 때만 초기화하도록 처리(안 그러면 이름만 고쳐도 정확한 지도 좌표가 날아가는 문제가 있었음)
- [x] 실제 Supabase 프로젝트 연동 및 데이터 적재 완료, 로그인/수정/조회 종단 간 테스트 및 검증 완료.
- [x] Upstash Redis를 사용하여 IP당 10분 내 5회 로그인 시도로 Rate Limit 구현 완료 (`lib/rateLimit.js`).
- [x] 비밀번호/시크릿은 `.env.local`과 Vercel 환경변수로 관리하며, 안전하게 기밀 유지 처리 완료.

### 2-5. 배포
- [x] `git init` 및 GitHub 원격 저장소(`ybkwon68-web/foodtrip`) push 완료
- [x] Vercel 프로젝트 연결 및 환경변수(6종) 등록 완료, 프로덕션 배포 완료
- [x] Supabase 프로젝트 생성, 스키마 마이그레이션 적용, `seed_supabase.py`로 데이터 적재 완료


## 3. 디렉토리 구조 (제안)

```
foodtrip/
├── crawler/
│   ├── crawl.py            # 목록/상세 크롤링
│   ├── regions.py          # 제목 기반 지역(시도/시군구) 사전 매칭
│   ├── enrich.py           # regions.py 실행 → seed/episodes.json에 region 채움
│   ├── naver_place.py      # 네이버 블로그 검색 + 장소위젯 파싱
│   ├── enrich_naver.py     # naver_place.py 실행 → seed/episodes.json에 restaurants 채움
│   ├── build_table.py      # 요약표(public/data/table.json) 생성
│   └── seed_supabase.py    # seed/episodes.json → Supabase episodes 테이블 최초 적재
├── seed/
│   └── episodes.json      # 최초 DB 적재용 스냅샷 (= public/data/episodes.json과 동일본, 배포용 사본)
├── supabase/
│   └── migrations/0001_init.sql  # episodes 테이블 스키마
├── lib/
│   ├── supabase.js         # 서비스 롤 Supabase 클라이언트 (서버 전용)
│   └── auth.js             # 관리자 세션 토큰 발급/검증
├── api/
│   ├── auth.js              # POST — 비밀번호 확인, 토큰 발급
│   ├── episodes.js         # GET — 전체 목록
│   └── episodes/[id].js    # GET 단건 / PUT 수정 (관리자 인증)
├── public/
│   ├── index.html, table.html
│   ├── style.css
│   └── script.js, table.js
├── package.json, vercel.json
├── .env.local.example      # 필요한 환경변수 형식 안내 (실제 값은 커밋 안 함)
├── checklist.md
├── context-notes.md
├── project.md
└── 구축계획.md
```

## 4. 예상 이슈 및 주의사항

- **크롤링 소요시간**: 목록 18페이지 + 상세 약 350여 회 요청 × sleep 1초 ⇒ 총 6~10분 예상
- **회차 결번/재방송**: 회차번호 중복·누락 가능성 있음 → 크롤링 후 데이터 검증 단계 필요
- **본문 인라인 스타일 충돌**: 원문에 폰트/정렬 인라인 스타일이 많아 웹페이지 디자인과 충돌할 수 있음 → 본문 영역만 CSS 리셋 적용
- **이미지 핫링크 차단 가능성**: `img.tvchosun.com` 이미지를 원본 URL로 그대로 참조하는 방식이므로, Referer 체크 등으로 로드가 막히는지 개발 중 실제 확인 필요 (막힐 경우 이미지 다운로드 저장으로 전환하는 대안 필요)
- **저작권**: 개인 아카이빙 목적이지만 Vercel 배포 시 공개 URL이 되어 재배포 성격을 띔 (project.md에도 명시된 리스크) → 필요 시 배포를 비공개로 전환하거나 접근을 제한하는 방안을 사용자가 판단
- **식당 정보 커버리지 한계**: 네이버 블로그 스크래핑으로 352건 중 220건(62%)만 확보됨. 오래된 회차는 관련 블로그 자체가 적어 구조적으로 채우기 어려움 — 나머지는 수동 입력(편집 기능)으로 보완해야 함
- **네이버 스크래핑 안정성**: 공식 API가 아닌 검색결과 페이지 직접 파싱이라, 페이지 구조 변경 시 크롤러가 깨질 수 있음. 주기적 재실행 시 매번 결과 검증 필요
- **DB/인증 추가로 인한 복잡도 상승**: 기존 "정적 파일만" 계획 대비 Supabase 설정, 환경변수 관리, 인증 로직이 추가됨 → 개인정보나 결제정보는 다루지 않지만 관리자 비밀번호 노출 시 데이터 훼손 가능성 있음(rate limit 등 최소 방어 필요)
- **네이버맵 링크 정확도**: 검색 링크 방식은 좌표 방식보다 부정확할 수 있음(동명 식당 등) → 식당명에 지역명을 함께 포함해 검색 정확도를 높임

## 5. 진행 단계 (상세는 checklist.md)

1. 크롤러 완성 및 검증 (1페이지 테스트 → 전체 실행)
2. 데이터 정제/검증 (`data/episodes.json`)
3. 정적 웹페이지 구현 (목록 → 상세 → 검색)
4. 로컬 브라우저 테스트
5. GitHub 저장소 연결 및 커밋
6. Vercel 배포 연결 및 확인
